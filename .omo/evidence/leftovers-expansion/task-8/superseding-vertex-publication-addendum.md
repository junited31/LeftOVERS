## Superseding Vertex publication addendum

This block is append-only text prepared for later insertion into the original `leftovers` plan after expansion F1 approves these exact bytes and hash. It supersedes only conflicting future-active language. It must not edit, delete, reword, re-check, or otherwise change any completed T1-T11 row or its evidence. The original GPT-5.6/OpenAI implementation, tests, deployment work, and Codex contribution remain truthful historical project history; old T12 evidence remains historical-only rather than final proof.

### TLDR for humans

LeftOVERS remains a local-first Android app, but current publication inference uses fixed Vertex AI Gemini models through the protected Cloud Run service and service-account Application Default Credentials. Before publication, the complete English/Korean expansion flow, Android APK, documents, screenshots, XML, release proof, and narrated media must all be bound to one reviewed source commit. The three actual publication records must be strict schema v2 and repeat that same source SHA. Any source change or recapture invalidates downstream evidence and reviews. YouTube Public upload and Devpost Submit remain separate action-time human authority gates.

### TLDR for machines

Future-active original-plan contract: preserve completed T1-T11 bytes/history; Vertex Gemini via Cloud Run ADC is current runtime; GPT-5.6/OpenAI and `store=false` are historical/parity truth only; require one existing ancestor `publication_source_sha`, exact post-source allowlist and clean tree, strict matching schema-v2 `SubmissionRecord`/`ReleaseProof`/`MediaEvidence`, T8-validated 11-state PNG/XML plus canonical artifact closure/digest, regenerated APK/media/release/publication proof, full final reviews, then stop before YouTube Public upload and again before Devpost Submit unless the user grants action-time authority.

### Remaining scope and guardrails

- Preserve Android local ownership of pantry, equipment, recipe snapshots, active cooking, feedback, notes, history, and retained final-dish photos. Firebase anonymous auth remains transport protection rather than an account, and Firestore remains HMAC-derived per-day quota IDs and counters only.
- Current recipe and cooking-photo inference uses the fixed Vertex routes already approved by the expansion plan, schema/domain validation, metadata-first safety handling, bounded request-owned retry budget, and one best-effort eligible fallback. Do not claim model availability, a guaranteed bill, food safety, or provider Zero Data Retention.
- The backend must not create an application-owned persistent copy of request bodies, pantry context, notes, model output, or cooking-photo bytes. Logs remain metadata-only and exclude secrets, tokens, bodies, pantry/history/preferences/notes, and images.
- Preserve the retained OpenAI implementation, dependency, secret, and `store=false` behavior as historical/parity context unless separate approval authorizes removal. Do not mount the OpenAI secret in the current Vertex production revision. Do not describe `store=false` as a Vertex setting, training/sharing control, abuse-log exemption, or Zero Data Retention guarantee.
- Do not create or enable optional synthetic sharing here. Any later synthetic-sharing experiment requires explicit approval, an isolated project, and generated non-user input/output only; real pantry, equipment, preferences, history, notes, and photos are forbidden.
- Do not add a recipe database, crawler, image generation, provider router, OpenAI-Vertex runtime fallback, account/cloud backup, new photo service, custom localization framework, or unapproved dependency.
- Never expose or commit secrets, tokens, billing/credit identifiers, user data/photos, request bodies, absolute local user paths, or unrelated worktree evidence.

### Verification, source, and recapture rules

- Freeze `publication_source_sha` at the approved product/config/script/document commit before creating the three actual records. It must be lowercase 40-hex, exist, and be an ancestor of current HEAD. Current tracked bytes must equal that commit except for the exact verifier allowlist, and the working tree must be clean.
- The only post-source paths are `.omo/evidence/leftovers-expansion/**`, `.omo/evidence/leftovers/task-12-publication/**`, `.omo/start-work/ledger.jsonl`, `.omo/boulder.json`, `.omo/plans/leftovers.md`, `dist/app-debug.apk.sha256`, `dist/release-debug-hooks.json`, `docs/submission.md`, `docs/demo-script.md`, and `docs/asset-provenance.md`. No prefix lookalike, fixture, verifier, Android/backend, README/privacy, expansion plan/draft, unnamed `.omo`, or unnamed `dist` path is allowed.
- `SubmissionRecord`, `ReleaseProof`, and `MediaEvidence` are three actual strict schema-v2 records with forbidden extras, noncoercing scalar types, canonical lowercase hashes, and identical `publication_source_sha`. Their source SHA precedes their evidence bytes; no record may self-reference its containing commit.
- Expansion T12 evidence must first pass the unchanged T8 validator: exactly 11 ordered canonical states, the ordered `04a`/`04b` subcaptures, required locales and UI markers, one source/APK/device/run binding, bounded nondecreasing UTC times, safe unique paths/hashes, valid PNG/IHDR/dimensions, parseable same-state XML, exact artifact closure, canonical JSON bytes, and stable lowercase `artifact_record_digest`.
- `artifact-manifest.json` never lists or digests itself. Every downstream F1-F5 receipt echoes the same digest. Any included-byte change changes or invalidates the digest.
- A missing, stale, corrected, or replacement capture is never patched by a reviewer. Route it to the exclusive T12 evidence owner, recapture from the same approved source or restart source approval if code/docs changed, regenerate and commit only allowlisted evidence, obtain a changed artifact digest, and invalidate/rerun all downstream review lanes.
- A verifier defect routes to T8, then requires a new approved source, authority/deployment verification, T12 evidence, and all final lanes. Any product/config/script/document defect routes to its owning expansion task and follows the same full chain.

### Superseding original T12 publication flow

1. Re-run source, backend, container, Android, security, privacy-document, and strict verifier gates with zero hidden skips. Confirm the deployed service uses the intended Vertex models through ADC, health/auth/quota behavior is current, logs are sanitized, and the approved source bytes still match.
2. On the named physical test device, clean-build debug and release APKs from the exact source-identical bytes. Reinstall the debug APK and repeat the complete canonical bilingual flow with synthetic data/photos only. Validate every original screenshot and UI XML through the unchanged T8 command and canonical artifact record; do not reuse old GPT-5.6-era APKs, captures, media, checksums, or proofs.
3. Generate `dist/app-debug.apk.sha256` from the exact judge-download APK. Scan the exact release APK and create strict schema-v2 `dist/release-debug-hooks.json` with the same source SHA, exact release APK SHA-256, and zero manifest, DEX, resource, and archive debug-hook matches. The release APK is proof of the production boundary; the debug APK remains the documented ADB-seedable judge artifact.
4. Record a new narrated demo below 180 seconds from approved source-bound captures. Disclose every edit/run transition and identify real Vertex-backed segments. Use only project UI and synthetic fixtures; show no personal content, notification, credential, or unrelated device/compositor surface. Watch the exact result end to end, fully decode it, confirm intelligible audio and visual continuity, and create strict schema-v2 `dist/leftovers-demo-media.json` with the identical source SHA, exact media SHA-256, observed duration, audio-stream count, and probe tool.
5. Update active README/privacy/submission/demo/provenance wording so Vertex is the current runtime, GPT-5.6/OpenAI is truthful historical contribution, old T12 proof is historical-only, local final-photo retention/server non-persistence/quota-only Firestore/metadata logs are clear, inference is billable and fallback best effort, image cautions are explicit, `store=false` distinctions are accurate, and optional synthetic sharing remains isolated and disabled.
6. Run the publication verifier against the three actual records. It must reject placeholders, v1/missing/extra/coerced records, mismatched/nonexistent/nonancestor/stale SHAs, forbidden or dirty drift, unreachable/private/example URLs, wrong repository ownership, checksum/media/proof mismatch, missing disclosures/English instructions/MIT/Codex/model history, and any historical evidence substitution.
7. Create or update GitHub release `v0.1.0-demo` only at the authorized publication stage, attach the exact debug APK and checksum, then independently read back the public release and downloads. Do not guess a URL or publish from a different SHA.
8. Stop immediately before YouTube Public upload and request action-time human confirmation. After approval, upload the exact verified media, check it signed out, and bind the real public URL. A local media pass and signed-out YouTube reachability are separate gates.
9. Populate the Apps for Your Life Devpost entry with truthful current description, technologies, source-bound repository/release/testing instructions, screenshots/video, privacy/safety distinctions, Codex contribution, historical GPT-5.6/OpenAI contribution, current Vertex runtime, and session ID. Keep status Draft while any required value or gate is unresolved.
10. Stop immediately before final Devpost Submit and request separate action-time human confirmation. After approval, submit once, verify the dashboard says `Submitted`, record the actual UTC timestamp and non-placeholder software URL in the strict submission record, and anonymously verify HTTP 200 plus visible project/challenge identity. Never infer submission from a click or authenticated draft page.

### T12 acceptance, QA, cleanup, and authority

- Acceptance requires public repository/MIT, exact downloadable debug APK/checksum, release-APK debug-hook absence proof, healthy protected backend, signed-out public narrated video under 180 seconds, actual session ID, English build/test/ADB instructions, complete privacy/model/Codex history, three matching strict-v2 records, source/visual/artifact verification, and an anonymously reachable submitted Devpost page.
- Happy QA is an agent-driven clean-device install following the published README, ADB seed/reset boundary check, exact-three Vertex recipe flow, synthetic cooking-photo advice, completion with locally retained final photo, restart/offline history, public release download/checksum, signed-out YouTube, and anonymous Devpost title/category/repository/video verification. Evidence must bind source/APK/media/artifact hashes and contain no sensitive text.
- Failure QA uses disposable copies only: Draft/missing URL/session/repository, v1/extra/coerced/mismatched records, stale APK/media/proof/capture, wrong device/locale/run/timestamp/XML binding, changed PNG/XML/file byte, artifact omission/extra/order/self-entry/self-digest/noncanonical bytes, source drift including simultaneous allowed evidence, private/example URLs, wrong checksum, silent/long/invalid media, failed health/auth/quota, and absent release hook proof must all fail with the exact field/path and cause no publication action.
- Cleanup removes only task-created local temp repos/files, device synthetic downloads/media rows/app cache/test retained photos/anonymous test user, and task container image/processes. Restore device network and English locale. Preserve requested cloud service/API/IAM/secrets and historical tracked evidence. Record removed versus retained resources without identifiers or secrets; prove no unrelated/parent worktree was scanned or staged.
- GitHub release mutation, YouTube Public upload, and Devpost Submit are external actions. They require their named action-time approvals; prior general implementation approval does not authorize them. Billing/IAM/API/deploy changes remain outside this resumed T12 unless separately authorized.

### Superseding final verification

- Re-run the unchanged T8 command and require the same source/APK hashes and artifact digest in all receipts.
- Require independent plan compliance, hands-on judge flow, code quality, security/privacy, context/history, functional visual, CJK precision, exact-SHA adversarial, and runtime-debug reviews at their lane-specific unconditional PASS/APPROVE threshold with zero blockers. No generic aggregate pass may replace a missing lane.
- Reviewers are read-only for source and captured evidence. Missing/replacement evidence routes to T12 and invalidates all receipts; verifier or source defects route through the owning-task chain and new source approval.
- Run Android unit/lint/build/instrumentation, backend tests, container build, filtered real-cloud E2E, secret scan, strict publication verifier, media probe/full decode, release-hook scan, public URL/readback, and Git cleanliness/diff checks. Every required command exits zero with no skipped/xfail tests added to manufacture a pass.
- Surface all final receipts and wait for user acceptance before plan completion bookkeeping. Preserve completed T1-T11 bytes, append this exact approved addendum without alteration, and keep original T12 unchecked until its publication work actually completes.

### Superseding success criteria

- The public MIT repository and `v0.1.0-demo` release expose the source-bound debug APK and matching checksum; the exact release APK proves all four debug-hook match counts are zero.
- The Android app completes the canonical bilingual local-first flow on the named device, retains final-photo/history state across restart/offline use, and sends only explicitly selected context/photo for current Vertex inference.
- The protected backend proves Firebase auth, atomic quota behavior, exact fixed Vertex model roles, schema/domain/safety checks, bounded best-effort fallback, ADC, metadata-only logs, no application-owned server persistence, sanitized evidence, and judge-accessible health.
- The three actual publication records are strict schema v2 with identical source SHA; the source is existent/ancestral/byte-identical under the exact allowlist; visual and artifact validation returns one stable digest echoed by every final lane.
- The fresh narrated demo has audio, is below 180 seconds, discloses edits and current Vertex-backed segments, uses only approved synthetic/project visuals, and is signed-out public only after authority.
- Active docs accurately distinguish current Vertex runtime, historical GPT-5.6/OpenAI contribution, local final-photo retention, transient inference/server non-persistence, quota-only Firestore, metadata logs, billable/best-effort behavior, image limits, `store=false`, provider governance, and disabled isolated synthetic sharing.
- All code/test/device/container/security/media/release/public/readback and independent review gates pass without stale or historical substitution, the Devpost dashboard is actually `Submitted` only after final authority, and its public software page is anonymously reachable.
