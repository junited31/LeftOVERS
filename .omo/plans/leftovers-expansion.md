# leftovers-expansion - Work Plan

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** A first-run setup that starts in English, guides people through kitchen equipment and pantry quantities, offers common ingredients and exact fractional counts, lets them switch between English and Korean, requires a recipe type, and clearly preserves their own finished-dish photo. Production recipe and cooking-photo requests will use fixed Gemini models through the protected cloud service while the phone remains the durable home for pantry, recipes, history, and final photos.

**Why this approach:** It extends the proven local-first flow and its existing validation seams instead of creating a recipe catalog, provider framework, translation system, or new database schema. Local fake-backed proof is completed before any billable cloud change, and every final publication artifact is bound to one reviewed source commit so stale evidence cannot pass.

**What it will NOT do:** It will not create a recipe database, crawler, dynamic provider router, cross-provider fallback, unit-conversion engine, sixth navigation tab, generated food imagery, or custom localization framework. It will not remove the retained OpenAI implementation or secret, perform cloud changes before confirmation, or reuse the old APK, demo, checksum, and release proof as final evidence.

**Effort:** XL
**Risk:** High - the change crosses Android lifecycle/localization, strict network contracts, billable model cutover, physical-device proof, and publication provenance.
**Deadline risk:** The official deadline is July 21, 2026 5:00 PM PDT / July 22, 2026 9:00 AM KST. The user explicitly directed that these features precede original T12/publication and has not granted posting authority, so this plan continues expansion knowingly without another prioritization question; missing the deadline remains a material risk.
**Decisions to sanity-check:** English is always the initial app locale; one saved equipment choice and pantry row are the adopted onboarding minimum; only explicit legacy local JSON decoders synthesize a missing recipe type as Meal; Vertex uses the global model location while Cloud Run stays in Seoul; transient retries are explicitly bounded and share one request budget; publication proof permits only named evidence/state/proof/document paths after the source commit.

Your next move: the start-work request already approved this plan; complete the required dual high-accuracy review, activate the expansion work, then execute until the explicit Google Cloud confirmation gate. Full execution detail follows below.

---

> TL;DR (machine): XL/high-risk native Android onboarding+i18n+RecipeKind+photo polish, FastAPI Vertex Gemini cutover with fixed bounded fallback, fail-closed publication-source proof, physical-device/visual/adversarial verification, then original T12 reactivation; knowingly proceeds before the 2026-07-22 09:00 KST deadline without posting authority because the user explicitly prioritized expansion first.

## Scope
### Must have
- Preserve the current request -> FastAPI -> exactly-three candidates -> Android validation/ranking -> selected Room snapshot topology, existing Firebase auth, Firestore per-UID/global quota counters, Pydantic validation, pantry/equipment/safety contracts, and local-only history/final photos.
- First-run order is Equipment -> Pantry quantity confirmation -> main app. Preferences must finish loading before routing; no bottom navigation appears before `ONBOARDING_COMPLETE=true`; equipment changes never mark onboarding complete; Equipment has Continue and Pantry has Finish, with each enabled only when the existing backend prerequisites (at least one equipment and one saved pantry row) are satisfied.
- Static staple suggestions only, with exact canonical ID -> default unit mapping: `eggs=count`, `milk=ml`, `butter=g`, `cheese=g`, `onion=count`, `garlic=count`, `rice=g`, `bread=count`, `flour=g`, `sugar=g`, `salt=g`, `black pepper=g`, `cooking oil=ml`, `tomato=count`, `potato=count`, `carrot=count`, `spinach=g`. These lowercase strings are the canonical IDs. Display localization uses exact case-sensitive, untrimmed, unnormalized code-point equality against that set regardless of whether the value was typed or suggestion-selected; exact `onion` localizes, while `Onion`, `Onion `, `onion `, and all other non-exact custom names remain verbatim. No provenance/schema field. Suggestions fill name/unit, never quantity, and save only through existing validation.
- Count-only quick chips `1/4`, `1/2`, `1`, `2` set existing parser inputs `0.25`, `0.5`, `1`, `2`; free decimal input remains. Onion 0.25 and 0.5 count must survive edit, process restart, and Room reload exactly.
- Keep five bottom destinations by replacing Equipment with Settings. Settings contains Kitchen equipment and Language. App default is English regardless of device locale; selectable locales are exactly English and Korean; changes render immediately and survive restart via AppCompat native app locales/string resources.
- Required `RecipeKind` values are Meal/Drink/Snack/Dessert (`meal|drink|snack|dessert` on the wire). No implicit default in recipe generation UI. The recipe request requires exactly `locale=en|ko` and `recipeKind`; locale drives generated human-language text. The response never echoes locale and requires only each candidate's matching `recipeKind` in addition to existing fields. Candidate kind is persisted/displayed through existing snapshot JSON/history; only legacy local snapshot JSON defaults missing kind to Meal.
- Existing user-owned final-photo gallery/private retention/history fallback remains, with an equally clear camera action, preview/ready state in completion, preview on immediate success, and one explicit `NonCancellable` ownership critical section. Cancellation before entry has no effects; once entered, a committed log must retain its referenced file. A failed transaction attempts compensation deletion; an orphan is tolerated only if that delete fails or the process dies, then minimal startup/retry reconciliation removes only Room-unreferenced files inside the exact managed retained directory and never touches referenced/unknown/out-of-directory files.
- Production uses one `GeminiAdapter` implementing the existing `AIAdapter`, `google-genai==2.12.1`, Vertex ADC, project `leftovers-019f706b`, location `global`, recipe primary/fallback `gemini-3.1-flash-lite`/`gemini-3.5-flash`, advice primary/fallback reversed, structured JSON schema, byte-part image input, metadata-first safety classification, explicit client close, and metadata-only logging.
- SDK retries are disabled with `HttpOptions(api_version="v1", retry_options=HttpRetryOptions(attempts=1))`. Every logical HTTP request creates its own frozen request-budget value; each state transition returns a new value passed explicitly through `parse_model_output`, the adapter method, and attempt context. Cached `GeminiAdapter`/client instances hold zero mutable budget, and no global, class field, closure, `ContextVar`, or thread-local owns it. Across the existing maximum three schema attempts, the hard cap is three ordinary primary invocations plus two additional primary transient-retry invocations after 250/500 ms, one fallback invocation, and six provider invocations total. Only `httpx.TimeoutException`, `httpx.ConnectError`, or `google.genai.errors.APIError` 429/500/503 consume transient budget; fallback is selected only after that allowed transient budget is exhausted, never retried, and stays consumed if its response is schema/domain-invalid. Any remaining schema repair attempts then use primary only. Every other error class has zero fallback.
- README, a new privacy document, submission/provenance/demo text accurately describe transient Vertex processing, on-device final photos, server non-persistence, redacted logs, billable inference, best-effort fallback, synthetic-share isolation, and the fact that `store=false` is not sharing/training/ZDR control.
- `SubmissionRecord`, `ReleaseProof`, and `MediaEvidence` are all strict schema v2 records: required `schema_version: Literal[2]`, `extra="forbid"`, strict strings/no coercion, and the same lowercase 40-hex `publication_source_sha`; v1, missing version, extra fields, and coerced scalar inputs fail closed. Expansion T12 creates only the active submission draft, visual/device evidence, APK reference, and canonical artifact record, while original T12 later creates and validates all three actual proof records. The verifier proves source existence, ancestry, and byte identity except for exact named post-source paths.
- Local fake-backed Android/backend/Docker/security checks complete before an explicit external confirmation checkpoint. After confirmation, mutate only `leftovers-019f706b`, keep Cloud Run in `asia-northeast3`, enable Vertex, bind only the actual Cloud Run identity to `roles/aiplatform.user`, redeploy, and run real structured recipe/advice plus the single filtered real-cloud device test.
- Fresh `SM-F711N` canonical 11-step English/Korean flow, with one numbered manifest entry and bound screenshot/XML capture pair for every step (and two ordered capture pairs inside the Onion quarter-to-half step), validated by the strict T8 verifier. T12 writes the canonical record only at `.omo/evidence/leftovers-expansion/task-12/artifact-manifest.json`; its lowercase SHA-256 canonical-record digest binds every F1-F5 receipt, and any recapture changes that digest and invalidates all lanes. Two independent visual reviewers (`PASS`, `HIGH`, blocking 0), independent adversarial exact-SHA verification, `omo:review-work` five PASS lanes, a separate runtime-debug hypothesis audit, cleanup receipts, explicit-file commits, and exact full SHA ledger records remain required.
- Any T12 capture/manifest/artifact or F1 hands-on-QA evidence replacement is owned exclusively by T12: the owner recaptures, commits only allowlisted evidence, regenerates canonical `artifact-manifest.json`, obtains a changed `artifact_record_digest`, and invalidates/reruns all F1-F5 lanes. Reviewers never patch evidence in place.
- After expansion final gates, mark expansion complete and reactivate original `leftovers` T12 unchecked. Do not rewrite completed T1-T11 rows; append a comprehensive superseding Vertex publication addendum that explicitly replaces every future-active TLDR, remaining scope, verification, T12, final-verification, and success contract while preserving GPT-5.6/OpenAI work as truthful historical contribution. Then regenerate publication proof and stop before YouTube/Devpost authority gates.

### Must NOT have (guardrails, anti-slop, scope boundaries)
- No recipe database/API/corpus/crawler, remote pantry catalog, provider router/model judge, dynamic model selection, OpenAI-Vertex runtime fallback, duplicated quota mechanism, background retry, or general retry framework.
- No Room entity/column/version/schema migration, compatibility layer for an unreleased build, custom i18n framework/repository, translated enum/UUID/equipment ID/canonical unit, sixth navigation item, free-form fraction parser, or g/ml conversion engine.
- No AI-generated finished-food image, Imagen, remote image search, recipe-card CDN, CameraX/custom preview, or new photo storage service.
- App quota, auth/permission/config/input, primary schema/domain failure, and safety never trigger fallback. If a transient-triggered fallback returns schema/domain-invalid output, it remains consumed and repairs return to primary. No more than one fallback invocation, hidden SDK retry, or provider-capacity guarantee.
- No deletion of `backend/app/openai_client.py`, `openai==2.46.0`, `OPENAI_API_KEY`, existing deployment, or any secret without separate post-parity approval. Do not mount the OpenAI secret into the post-cutover production revision, but do not destroy it.
- No creation of `leftovers-synthetic-share`, no input/output-sharing enablement, and no real pantry/equipment/history/preferences/notes/photo sent to a synthetic-sharing project in this plan.
- No API enablement, IAM change, Cloud Run config/revision/deploy, actual Vertex request, or `RealCloudE2eTest` before the explicit cloud confirmation todo completes. No access/change outside `leftovers-019f706b`.
- No push, PR, merge, tag, release, upload, or Devpost submit until their specified gates. Old `.omo/evidence/leftovers/task-12-*`, `dist/*`, old screenshots/video/checksum/release proof, and old publication copy are historical-only, never final proof.
- Never log/commit token/key/secret, billing or credit identifier/expiry, request body, image bytes, pantry list, actual user photo, temp device file, local username/absolute path, or sensitive untracked parent-worktree evidence. Never scan/stage from `D:/workspace/develop/LeftOVERS` or traverse its `impl-link` junction.

## Verification strategy
> All QA, test execution, device operation, artifact inspection, and review are agent-executed. Human participation is limited to explicit authority gates for Google Cloud, YouTube Public upload, and Devpost Submit.
- Test decision: TDD baseline -> new failing product/behavior/verifier test -> implementation -> green applies to T1-T9 only, where production behavior, verifier behavior, or local integration guards change. T10 is a human authority gate and introduces no failing product test; it reuses committed T9 guard fixtures. T11 performs external execution with previously RED-tested scripts plus live negative probes and never changes source to manufacture RED. T12 is evidence capture/recapture and uses freshness, stale-artifact, manifest, and adversarial negative fixtures without introducing a failing product test. Keep each todo's required `Baseline then RED:` field, but interpret it according to this scope.
- Evidence root: `.omo/evidence/leftovers-expansion/task-<N>/` with `baseline.txt`, `red.txt`, `green.txt`, `manual.json`, `cleanup.json`, device XML/PNG, fake-call ledger, and adversarial report as applicable. Every JSON record includes `git rev-parse HEAD`, command, exit code, artifact SHA-256, and zero secret/user-data fields.
- Android source gate from repository root: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease :app:compileDebugAndroidTestKotlin --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location`.
- Safe local connected gate (never unfiltered): build a comma-joined class list containing `LeftoversFlowE2eTest`, `CookingPhotoFlowTest`, `MealCompletionFlowTest`, `DatabaseMigrationTest`, `InventoryCompletionInstrumentedTest`, `HistoryScreenTest`, `PhotoPickerTest`, `PantryEquipmentTest`, `RecipeScreenTest`, and new `OnboardingSettingsTest`; invoke `Push-Location android; .\gradlew.bat :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=$localClasses" --no-daemon --console=plain; Pop-Location`; parse all connected XML totals and assert failures/errors/skipped are zero and no testcase contains `RealCloudE2eTest`.
- Backend gate: `backend\.venv\Scripts\python.exe -m pytest -q backend\tests`; Docker gate uses the exact SHA tag `leftovers-api:verify-<first12>` with `docker build --pull=false --no-cache`, then removes only that tag after inspection. Secret gate is `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\scan_secrets.ps1` from `D:/workspace/develop/LeftOVERS-impl` only.
- Provider fake ledger must prove model names, location, structured schema aliases, image part, safety classification, ordinary-primary/schema-slot count <=3, extra transient-primary retry count <=2, total primary count <=5, fallback count <=1, total provider count <=6, delays `[0.25,0.5]`, fallback-consumed state after schema-invalid fallback output, quota count 1, zero fallback trigger for forbidden classes, client close, temporary-photo closure, and sanitized logs. A passing summary without captured call records is invalid.
- Manual QA is agent-driven on serial `SM-F711N` serial `R5CR91DXA8R`; reset app data, use only synthetic pantry/photo fixtures, capture PNG plus UI hierarchy XML for every required state, hash them into one manifest, and clean only files matching the run prefix. Device state, Gradle, Docker, evidence, and Git writers never overlap.
- External confirmation is a human authority gate, not QA: T9 names `approved_source_sha` as the exact product/source commit. The executor stops and asks in Todo 10 without writing; after affirmative only, one commit may add the exact T10 authority receipt, ledger event, and Boulder plan-state binding. Evidence may prove only that this executor issued no live Google Cloud invocation since activation. T11 invalidates the receipt unless all tracked product/config/scripts/docs bytes still match the T9 manifest and the post-question diff contains only the three named authority paths.
- Final claims require source candidate SHA byte identity, clean worktree, the exact canonical `artifact-manifest.json` digest, functional/design and CJK visual reviews both `PASS`/`HIGH`/blocking 0, independent adversarial confirmation, five `omo:review-work` PASS receipts, runtime-debug hypothesis results, cleanup, and a ledger event bound to the full 40-character source SHA plus artifact-record digest.

### Activation bootstrap (A0 - required before Todo 1)
1. From `D:/workspace/develop/LeftOVERS-impl`, re-read `.omo/plans/leftovers.md`, `.omo/boulder.json`, and `.omo/start-work/ledger.jsonl` through EOF; verify the original `leftovers` plan is active with T12 unchecked, stale T12 preparation evidence is not completion proof, the branch is `feat/leftovers-mvp`, baseline `2010872b111a44e3c841abcb512d9f2879d70074` is an ancestor, and the only intended dirt is the reviewed expansion plan/draft.
2. Verify both high-accuracy plan reviews unconditionally approve the same plan SHA while this draft still records review state. Commit only `.omo/plans/leftovers-expansion.md` and `.omo/drafts/leftovers-expansion.md` as `docs(omo): add reviewed LeftOVERS expansion plan` with footer `Plan: .omo/plans/leftovers-expansion.md`.
3. In one serialized root-state edit, set original `leftovers.status=paused`, add/set `leftovers-expansion.status=active`, set `active_work_id=leftovers-expansion`, preserve all prior sessions, append root session `codex:019f82b0-8db6-76c0-a475-7974d14896cc`, and bind worktree `D:/workspace/develop/LeftOVERS-impl`. Append one sanitized ledger event declaring old original-T12 APK/demo/screens/checksum/proofs historical-only and naming the reviewed plan/source commit; include no account, token, billing, secret, user data, absolute username path, or parent-worktree evidence.
4. Re-read Boulder and ledger after the write, require exactly one active work ID and the expected last ledger event, then commit only `.omo/boulder.json` and `.omo/start-work/ledger.jsonl` as `chore(omo): activate LeftOVERS expansion` with footer `Plan: .omo/plans/leftovers-expansion.md`. Todo 1 cannot begin until both A0 commits exist and the worktree is clean.

## Execution strategy
### Parallel execution waves
- Bootstrap A0 is a serialized root gate and must complete before any implementation worker starts. It preserves user changes and never enumerates/stages parent-worktree paths.
- Wave 1, local implementation: one Android UI writer serially executes T1 -> T2 -> T3 -> T5 -> T6. One backend writer executes T4 -> T7 and may run in parallel with that Android chain. T8 begins after T7 and may overlap only Android work whose file list is disjoint. No two agents edit the same file, run Gradle/device/Docker, or write evidence/Git concurrently.
- Wave 2, local convergence: T9 runs after T1-T8, returns defects to owners, then binds the final clean product/source commit as `approved_source_sha` with a complete tracked-byte manifest before its evidence-only commit.
- Wave 3, authority and cloud: T10 asks without writing; after affirmative its only commit is authority.json+ledger+Boulder bound to `approved_source_sha`. T11 byte-compares the manifest and exact three-path authority diff before exclusively owning external cloud/device activity.
- Wave 4, final evidence: T12 sets `publication_source_sha == approved_source_sha`, never edits source, and exclusively owns capture/manifest/artifact evidence. Source defects restart owner -> T9 -> new T10 -> T11 -> T12. Evidence recapture commits only allowlisted T12 evidence and invalidates all F1-F5.
- Final review is staged by ownership: F1 runs five review-work lanes, F2/F3 visual/CJK, F4 adversarial, F5 runtime. Reviewers write only reports. Every evidence deficiency routes to T12 and reruns all five; every code/contract defect follows the full owning chain. Completion runs only after all approve and the user accepts results.
- Every worker rereads this plan and the listed references immediately before editing, reports exact files, and stops at its acceptance boundary. Root orchestrator delegates all investigation/design/implementation/testing/QA/review and only serializes shared state/commits.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| A0 | reviewed plan | T1,T4 | none |
| T1 | A0 | T2,T5,T6,T9 | T4 |
| T2 | T1 | T3,T9 | T4,T7 |
| T3 | T2 | T5,T9 | T4,T7,T8 |
| T4 | A0 | T5,T7,T9 | T1,T2,T3 |
| T5 | T1,T3,T4 | T6,T9 | T7,T8 |
| T6 | T5 | T9 | T7,T8 |
| T7 | T4 | T8,T9 | T2,T3,T5,T6 |
| T8 | T7 | T9,T12 | T3,T5,T6 |
| T9 | T1-T8 | T10 | none |
| T10 | T9 | T11 | none |
| T11 | T10 confirmed | T12 | none |
| T12 | T11 | F1-F5 | none |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. Establish native English/Korean app locales and resource every user-visible string
  - What to do / Must NOT do: First run current Android unit tests and existing focused connected UI classes unchanged. Add `androidx.appcompat:appcompat:1.7.0`, make `MainActivity` an `AppCompatActivity`, use an AppCompat-compatible no-action-bar theme, add default English `values/strings.xml`, Korean `values-ko/strings.xml`, and `xml/locales_config.xml` containing only `en` and `ko`; set `<application android:localeConfig="@xml/locales_config">`, resource the manifest label, and declare `androidx.appcompat.app.AppLocalesMetadataHolderService` with `android:enabled="false"`, `android:exported="false"`, and child metadata `android:name="autoStoreLocales" android:value="true"`. On the main thread, call `super.onCreate(savedInstanceState)` first; then, before `setContent`, if `AppCompatDelegate.getApplicationLocales().isEmpty`, call `AppCompatDelegate.setApplicationLocales(LocaleListCompat.forLanguageTags("en"))`. T1 owns only this locale bootstrap/resource harness and moving user-facing strings/content descriptions/format strings from `MainActivity.kt`, `RecipeScreen.kt`, `CookingScreen.kt`, `MealCompletionScreen.kt`, `HistoryScreen.kt`, and `CookingAdviceJson.kt` to resources while keeping test tags and wire/data values unchanged. Do not add Settings, onboarding routing, a locale selector, a locale repository, DataStore locale key, wrapper framework, translated enum/UUID/equipment/canonical-unit value, or broad design-system refactor.
  - Parallelization: Wave 1, sole Android UI writer | Blocked by: A0 | Blocks: T2,T5,T6,T9 | Can parallelize with: T4 only.
  - References: `android/app/build.gradle.kts:23-35,64-94`; `android/app/src/main/AndroidManifest.xml:5-28`; `android/app/src/main/res/values/styles.xml:1-7`; `MainActivity.kt:82-116,121-293,295-499`; `RecipeScreen.kt:55-225`; `CookingScreen.kt:185-235`; `MealCompletionScreen.kt:70-225`; `HistoryScreen.kt:68-214`; `CookingAdviceJson.kt:1-60`; AppCompat per-app locale docs `https://developer.android.com/guide/topics/resources/app-languages`.
  - Baseline then RED: capture unchanged `:app:testDebugUnitTest`, `PantryEquipmentTest`, `RecipeScreenTest`, `MealCompletionFlowTest`, and `HistoryScreenTest` in `task-1/baseline.txt`. Add only a narrow `AppLocaleBootstrapTest`/compiled-resource harness proving current `ko-KR` first launch is not application-English, localeConfig/metadata are absent, the delegate call is not ordered after `super.onCreate` and before `setContent`, and hardcoded text cannot switch under a test-only delegate locale change; capture only these expected failures in `task-1/red.txt`. No onboarding/settings test is added in T1.
  - Acceptance criteria: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease :app:compileDebugAndroidTestKotlin --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location` exits 0. `aapt2 dump resources` or compiled-resource inspection proves `en` and `ko` only, manifest label is `@string/app_name`, release manifest includes native locale config and no custom locale service beyond AppCompat metadata. A source scan over the six named Kotlin files rejects remaining Hangul or hardcoded English literals passed directly to `Text`, labels, supporting text, semantics, and user-visible error constructors; canonical model/prompt/test-fixture data is explicitly excluded and reviewed.
  - QA happy: the agent clears app data on `R5CR91DXA8R`, keeps device locale `ko-KR`, installs/launches the debug APK, and asserts English resource rendering plus AppCompat application locale `en`; a test-only harness sets `ko`, recreates, and asserts Korean resources and persistence, then restores `en`. Capture `task-1/manual-en.png|xml`, `manual-ko-harness.png|xml`, and `manual.json` with locale lists and hashes. No Settings UI is expected in T1.
  - QA failure: the agent injects unsupported `ja` through the test-only AppCompat harness and restarts; PASS only if no crash/raw ID occurs, supported English resources render, and IDs/units remain canonical. Evidence `task-1/failure-unsupported-locale.json`.
  - Adversarial classes: stale locale after recreation/cold start; device-locale bleed-through; missing resource causing raw ID; translated equipment wire value; CJK clipping; misleading connected success. Record XML test totals and exact APK SHA in `task-1/adversarial.json`.
  - Cleanup: restore the device app locale to English, remove only `leftovers-expansion-task-1-*` device captures, stop Gradle started by the task, and record zero temp files/processes in `task-1/cleanup.json`.
  - Commit: Y | `feat(locale): add native English and Korean UI` with footer `Plan: .omo/plans/leftovers-expansion.md`; stage only the explicit Gradle/manifest/resource/Kotlin/test files and task-1 evidence.

- [x] 2. Gate first launch through Equipment and Pantry, then replace Equipment navigation with Settings
  - What to do / Must NOT do: Reuse `ONBOARDING_COMPLETE`, `EquipmentChecklist`, `PantryList`, and `PantryForm`. Treat `preferences == null` as loading, never as incomplete. Adopt the explicit minimum of one saved equipment choice and one saved pantry row. When incomplete, render no main `Scaffold`/bottom navigation and keep a saveable two-step onboarding state: Equipment first with Continue disabled until the equipment minimum is saved; Pantry second with the existing list/form and Finish disabled until the pantry minimum exists. Only Finish writes `ONBOARDING_COMPLETE=true`; equipment toggles update only `EQUIPMENT_IDS`. On completion enter main Pantry. Keep exactly five bottom items by replacing `AppScreen.EQUIPMENT` with `SETTINGS`; Settings embeds Kitchen equipment and the English/Korean selector. Its event handler calls `AppCompatDelegate.setApplicationLocales` on the main thread with exactly `en` or `ko`; AppCompat owns persistence. Do not add Navigation Compose, a ViewModel layer, migration/reset compatibility, Skip, sixth tab, or duplicate equipment screen.
  - Parallelization: Wave 1, same sole Android UI writer after T1 | Blocked by: T1 | Blocks: T3,T9 | Can parallelize with: T4,T7 after file ownership check.
  - References: `MainActivity.kt:103-140,146-210,211-280`; `LeftoversPreferenceKeys.kt:10-18`; `DebugSeedReceiver.kt:50-60`; `backend/app/models.py:93-100` (at least one pantry/equipment); `PantryEquipmentTest.kt`; `LeftoversFlowE2eTest.kt`; verified loading-state risk at `MainActivity.kt:132-133`.
  - Baseline then RED: run existing `PantryEquipmentTest` and `LeftoversFlowE2eTest` against a debug-reset state and record baseline. Add `OnboardingSettingsTest` that clears DataStore/Room and asserts Equipment is first, bottom-nav tags are absent, a toggle alone does not complete onboarding, Continue advances, empty Pantry cannot Finish, a saved row enables Finish, recreation enters main only after Finish, Settings replaces Equipment, and its en/ko selector changes immediately and persists; current code must fail these checks and the failure output goes to `task-2/red.txt`.
  - Acceptance criteria: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.junited31.leftovers.ui.OnboardingSettingsTest,com.junited31.leftovers.ui.PantryEquipmentTest" --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location` exits 0; parsed XML has zero failures/errors/skips. Room schema JSON remains byte-identical and `LeftoversDatabase.VERSION == 1`. A source assertion finds exactly five `NavigationBarItem` calls and no `AppScreen.EQUIPMENT`; equipment toggle code contains no write to `ONBOARDING_COMPLETE`.
  - QA happy: clear app data, launch on the Flip3, select Gas burner and Basic cookware, Continue, add one pantry row, Finish, assert Pantry main plus five bottom destinations, open Settings, verify both Kitchen equipment and Language are reachable, restart and assert onboarding does not reappear. Evidence `task-2/equipment.png|xml`, `pantry.png|xml`, `settings.png|xml`, `manual.json`.
  - QA failure: (a) start with preferences still loading and prove no one-frame onboarding/main flash via a 30 fps short capture; (b) toggle equipment then kill/relaunch before Continue and prove Equipment remains first and bottom nav absent; (c) reach Pantry with zero saved rows and prove Finish disabled with no DataStore completion write. Evidence `task-2/failure-loading.mp4`, `failure-restart.xml`, `failure-empty.json`.
  - Adversarial classes: stale preference flow, process recreation, rapid double Finish, empty prerequisite, bottom-nav escape, debug reset, misleading UI-only assertion. Verify DataStore and Room directly in instrumentation and record `task-2/adversarial.json`.
  - Cleanup: close ActivityScenario, clear task-created Room/DataStore/device media, leave app locale English, remove only task-2 capture prefix, and record restored device/network state.
  - Commit: Y | `feat(onboarding): require kitchen and pantry setup` with footer `Plan: .omo/plans/leftovers-expansion.md`; include direct tests/evidence, no unrelated UI cleanup.

- [x] 3. Add local staple suggestions and exact count fraction chips to the existing pantry form
  - What to do / Must NOT do: Define one static list beside `PantryForm` with the exact canonical ID -> default unit pairs `eggs=count`, `milk=ml`, `butter=g`, `cheese=g`, `onion=count`, `garlic=count`, `rice=g`, `bread=count`, `flour=g`, `sugar=g`, `salt=g`, `black pepper=g`, `cooking oil=ml`, `tomato=count`, `potato=count`, `carrot=count`, `spinach=g`, plus localized labels. Suggestion selection fills the canonical ID and mapped unit, leaves quantity blank, and performs no DAO call; custom edits persist exactly what the user typed. At every pantry display boundary, localize when and only when persisted `name` equals one canonical ID by Kotlin case-sensitive `==` on the unchanged string: do not trim, lowercase/case-fold, apply Unicode normalization, or consult selection provenance. Therefore suggestion-selected or manually typed exact `onion` localizes identically, while `Onion`, `Onion `, `onion `, Unicode lookalikes, and other custom values render verbatim. Add no provenance flag, entity column, converter field, migration, table/API/catalog, or duplicate system. Count chips keep the existing exact decimal parser behavior.
  - Parallelization: Wave 1, same sole Android UI writer after T2 | Blocked by: T2 | Blocks: T5,T9 | Can parallelize with: T4,T7,T8 if disjoint.
  - References: `MainActivity.kt:295-351,353-459,500-507`; `QuantityParser.kt:5-15`; `Entities.kt:8-16`; `LeftoversDatabaseTest.kt:68-83`; `PantryEquipmentTest.kt`; T1 localized resources.
  - Baseline then RED: run database/pantry tests and capture existing decimal behavior. Add tests for all 17 mappings, zero DAO write on suggestion selection, blank quantity, chip mapping/visibility, exact 250/500 persistence, and a display table proving both suggestion-saved and manually typed exact lowercase `onion` render Onion/양파, while `Onion`, `Onion `, `onion `, and a Unicode lookalike remain byte-verbatim in EN/KO. Capture failing output in `task-3/red.txt` before implementation.
  - Acceptance criteria: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest --tests '*LeftoversDatabaseTest' --tests '*Pantry*' :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.junited31.leftovers.ui.PantryEquipmentTest" --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location` exits 0 with clean XML. Instrumentation asserts Room exact bytes: typed `onion` stays `onion` and localizes; `Onion ` stays `Onion ` and never localizes. Quantity 250 then 500 survives recreation. Source/schema assertions find no provenance/entity/converter/migration field, normalization/trim/lowercase call in canonical matching, or Room version/schema change.
  - QA happy: the agent saves suggestion Onion at 1/4, edits to 1/2, restarts, switches Korean, and observes `양파 0.5 개`; then manually types exact `onion` and confirms the same localized display. Evidence `task-3/suggestions-en.png|xml`, `fraction-half-ko.png|xml`, `typed-onion-ko.png|xml`, and `manual.json` includes Room bytes.
  - QA failure: manually save `Onion ` and `onion ` and prove both remain exactly verbatim across locale/restart; select Milk and prove count chips absent; save no quantity and prove no DAO change; free decimal `0.375` remains 375 milli-units. Evidence `task-3/failure-near-match.json`, `failure-no-quantity.json`, `failure-unit.xml`, `free-decimal.json`.
  - Adversarial classes: trim/case-fold/Unicode-normalization leakage, typed canonical collision, locale switch, rapid suggestions, stale version, hidden auto-save, rounding, display-only persistence. Record exact UTF-8/DAO bytes and XML totals in `task-3/adversarial.json`.
  - Cleanup: remove only task-created pantry rows/captures, preserve normal app preferences, stop task Gradle/ADB operations, and record zero task temp files.
  - Commit: Y | `feat(pantry): suggest staples and exact fractions` with footer `Plan: .omo/plans/leftovers-expansion.md`.

- [x] 4. Make locale and RecipeKind required fail-closed FastAPI recipe contracts
  - What to do / Must NOT do: Add Pydantic `RecipeLocale(StrEnum)` values `en|ko` and `RecipeKind(StrEnum)` values `meal|drink|snack|dessert`. Add required `locale` and `recipeKind` to `RecipeGenerateRequest`, and required `recipeKind` to every `RecipeCandidate`; no network defaults. Locale is request-only and drives the prompt's human-language title, missing-ingredient names, and steps; neither the response envelope nor candidate echoes locale. Extend `validate_recipe_bindings` to reject candidate/request kind mismatch before success while preserving exactly-three, cuisine/technique diversity, equipment, pantry ID/version/unit/amount checks. Update deployment request fixtures/preflight and backend fakes. Advice schema remains enum-based and is localized client-side; do not add an unrelated advice-locale contract. Do not loosen `extra="forbid"`, duplicate validators in routes, change quotas, add a response locale, or add provider logic here.
  - Parallelization: Wave 1 backend writer | Blocked by: A0 | Blocks: T5,T7,T9 | Can parallelize with: T1-T3.
  - References: `backend/app/models.py:27-34,93-128,202-242`; `backend/app/main.py:181-197,247-262`; `backend/app/prompts.py`; `backend/tests/conftest.py`; `backend/tests/test_recipe_generation.py`; `backend/tests/test_api_boundaries.py`; `scripts/tests/fixtures/recipe-request.json`; `scripts/deploy_backend.ps1:65-98,145-157`.
  - Baseline then RED: run `backend\.venv\Scripts\python.exe -m pytest -q backend\tests\test_recipe_generation.py backend\tests\test_api_boundaries.py` unchanged. Add exactly seven invalid rows: request missing locale, request unknown locale, request missing kind, request unknown kind, candidate missing kind, candidate unknown kind, and candidate kind mismatching the request. Add one valid Korean Dessert response that contains no locale field and passes every old invariant; current models must fail the desired contract and output goes to `task-4/red.txt`.
  - Acceptance criteria: `backend\.venv\Scripts\python.exe -m pytest -q backend\tests\test_recipe_generation.py backend\tests\test_api_boundaries.py backend\tests\test_openai_client.py` exits 0. The four invalid request rows return typed 422 before provider invocation; the three invalid candidate rows exhaust only the existing primary schema/domain repair path and end `ModelValidationFailed` with zero fallback selection. Valid fixtures contain exactly three diverse matching-kind candidates and no response locale. `RecipeGenerateRequest.model_json_schema(by_alias=True)` requires `locale` and `recipeKind`; candidate schema requires `recipeKind` and contains no `locale`; response-envelope schema contains no `locale`.
  - QA happy: launch local FastAPI with injected fake, POST an authenticated `locale=ko`, `recipeKind=dessert` fixture, assert 200, exactly three candidates, every kind `dessert`, Korean title/ingredient/step fixture text, and unchanged pantry/equipment bindings. Evidence `task-4/happy-response.json` with synthetic data only.
  - QA failure: execute the exact seven-row matrix; assert the four request failures are generic 422 with provider count 0 and the three response failures are generic typed model-validation failures with primary repair counts only, fallback count 0, no echoed locale, no traceback, and no response leakage. Evidence `task-4/failure-matrix.json`.
  - Adversarial classes: extra fields, case drift (`Meal`, `KO`), Unicode lookalikes, missing field, candidate mismatch, invalid set masked by other valid candidates, prompt injection inside notes, misleading 200 before domain validation. Record verdict/call ledger in `task-4/adversarial.json`.
  - Cleanup: stop only task uvicorn, free its exact port, delete synthetic temp request files, clear dependency overrides, and record no Docker/cloud/device activity.
  - Commit: Y | `feat(api): require recipe kind and locale` with footer `Plan: .omo/plans/leftovers-expansion.md`; implementation and direct backend/script-fixture tests stay together.

- [x] 5. Require RecipeKind in Android generation and preserve it in existing snapshot JSON/history
  - What to do / Must NOT do: Add Android `RecipeKind` with the four canonical wire values and localized labels. Add it to `RecommendationCandidate`. `RecipeScreen` holds nullable selection, shows exactly four single-select controls before Generate, and keeps Generate disabled until pantry, equipment, and one kind are present. Derive request locale from current AppCompat app locales and normalize to exactly `en|ko`; pass locale and selected kind into `RecipeJson.request`. Change `RecipeJson.response` to accept the requested kind and fail closed before ranking when candidate kind is absent, unknown, or different. Make `RecipeSteps.recipeKind: RecipeKind` required with no default in the data class and every normal constructor/new encoder path. Only the explicitly named legacy local array decoder and legacy local object decoder may synthesize `RecipeKind.MEAL` when the historical key is absent; the network decoder and new local JSON never default. Copy candidate kind in `RecipeSnapshotSaver` and display its localized label in recipe cards/selected snapshot/history detail. Keep `LeftoversDatabase.VERSION=1`, schema JSON byte-identical, and `RecommendationRanker` behavior unchanged.
  - Parallelization: Wave 1, sole Android UI writer after T3 and backend contract T4 | Blocked by: T1,T3,T4 | Blocks: T6,T9 | Can parallelize with: T7,T8.
  - References: `RecipeJson.kt:11-47,75-101`; `RecipeModels.kt:20-68`; `RecipeScreen.kt:55-170,175-225`; `DomainModels.kt:39-48,66-73`; `JsonConverters.kt:29-63,156-182`; `RecipeSnapshotSaver.kt:12-47`; `HistoryScreen.kt:112-170`; `LeftoversDatabase.kt:9-29`; `RecommendationRanker.kt`; tests `RecipeJsonTest.kt`, `RecipeSnapshotSaverTest.kt`, `HistoryRepositoryTest.kt`, `RecipeScreenTest.kt`, `HistoryScreenTest.kt`.
  - Baseline then RED: run all five named unit/connected tests unchanged and record old exactly-three/ranking/snapshot/history behavior. Add failing tests for Generate disabled with no kind, one-of-four selection, required request aliases/wire values/locale, response candidate kind missing/unknown/mismatch rejection, explicit absence of response locale, snapshot/history retention, and legacy local JSON Meal default. Capture failure output before implementation in `task-5/red.txt`.
  - Acceptance criteria: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest --tests '*RecipeJsonTest' --tests '*RecipeSnapshotSaverTest' --tests '*HistoryRepositoryTest' --tests '*RecommendationRankerTest' :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.junited31.leftovers.ui.RecipeScreenTest,com.junited31.leftovers.history.HistoryScreenTest" --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location` exits 0 with zero XML failures/errors/skips. Hash `android/app/schemas/com.junited31.leftovers.data.LeftoversDatabase/1.json` before/after and require equality. A test decodes legacy array and legacy object to Meal locally, while the same missing field in HTTP JSON is Invalid.
  - QA happy: in English choose Drink, generate a three-card fake response, save one, complete enough flow to open history detail, and assert Drink is shown in card/snapshot/history; switch Korean, choose Dessert, and assert only labels/content locale change while wire/DB values remain `dessert`. Evidence `task-5/kind-unselected.png|xml`, `drink-results.png|xml`, `history-drink.png|xml`, `dessert-ko.png|xml`, `manual.json`.
  - QA failure: inject missing, `Meal`, `unknown`, and mismatched `snack` candidate kind for a `drink` request; each must show the existing typed invalid-set UI, create zero snapshots/sessions, and leave ranker invocation/result ledger empty. Evidence `task-5/failure-network-kind.json`.
  - Adversarial classes: locale process restart, no-selection click race, multiple selection semantics, network default leakage, legacy/network decoder confusion, Room schema drift, ranking mutation, translated wire value. Record direct JSON/DAO assertions in `task-5/adversarial.json`.
  - Cleanup: clear injected fake responses and task rows/photos/captures, restore English, verify no DB schema file changed, and stop only task-owned Gradle/ADB work.
  - Commit: Y | `feat(recipes): require and retain recipe kind` with footer `Plan: .omo/plans/leftovers-expansion.md`.

- [x] 6. Make the existing user final-photo flow obvious and cancellation-safe
  - What to do / Must NOT do: Keep the existing gallery, JPEG compression, app-private retention, history image/missing/empty states, and startup sweep. Add a completion camera action by reusing `PhotoContracts.takePicture`/`PhotoLifecycle.createManagedPhoto`/`compressCamera`, show a real preview plus explicit replace/remove/ready semantics in the completion form, and show the retained photo on immediate success. Extend `CompletionResult.Success` with nullable retained path and have `MealCompletionStore` return it. Define one ownership critical section entered only after all cancellable validation/preparation. Cancellation before entry has no effects. Once entered, wrap only retained-file ownership transfer, Room completion, and best-effort compensation deletion in `withContext(NonCancellable)`: if Room commits, return only while its referenced file exists and never let caller cancellation delete it; if Room fails, delete the unreferenced retained file, but explicitly allow an orphan only when that deletion fails or the process dies. Add minimal startup/retry reconciliation using existing lifecycle wiring: load/canonicalize all Room final-photo references first, enumerate only direct children of the canonical app-managed retained directory whose names match the exact managed prefix, and delete only managed files absent from the Room reference set. A delete failure stays for retry on the next startup; never delete a referenced path, unknown filename, symlink escape, or path outside that directory. Do not claim cross-resource filesystem+Room atomicity, swallow cancellation outside this narrow section, generate/search/upload imagery, add CameraX, or store photos server-side.
  - Parallelization: Wave 1, sole Android UI writer after T5 | Blocked by: T5 | Blocks: T9 | Can parallelize with: T7,T8.
  - References: `CookingScreen.kt:138-183,185-218`; `MealCompletionScreen.kt:70-111,113-225`; `MealCompletionStore.kt:8-31`; `PhotoLifecycle.kt:18-84,86-150`; `DomainModels.kt:58-64,102-114`; `Daos.kt:115-170`; `HistoryScreen.kt:123-154`; tests `CompleteMealFlowTest.kt`, `MealCompletionFlowTest.kt`, `HistoryScreenTest.kt`, `PhotoLifecycleTest.kt`.
  - Baseline then RED: run all four named test groups to prove gallery/retention/history fallback currently pass. Add failing deterministic barriers for cancellation (a) before entry, (b) during retained-file/Room work, and (c) immediately after commit; Room failure plus successful/failed compensation delete; simulated process death after retain-before-commit; startup reconciliation; delete failure then next-start retry; referenced, unknown-name, symlink, and out-of-directory preservation; camera/preview/no-photo/missing-file behavior. Capture direct file/DB state in `task-6/red.txt`; do not accept only a UI assertion.
  - Acceptance criteria: `Push-Location android; .\gradlew.bat :app:testDebugUnitTest --tests '*CompleteMealFlowTest' --tests '*PhotoLifecycleTest' :app:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.junited31.leftovers.cooking.MealCompletionFlowTest,com.junited31.leftovers.history.HistoryScreenTest" --no-daemon --console=plain; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Pop-Location` exits 0. Tests assert: cancel before entry => no log/no retained file; cancel during the non-cancellable section waits for one valid terminal state; cancel after commit => one log and existing referenced file; Room failure+delete success => no log/no file; Room failure+delete failure or process death may leave one unreferenced managed orphan, which startup/retry reconciliation removes once deletion succeeds. Reconciliation never removes referenced, unknown-name, symlinked, or out-of-directory files. Success bytes match success/history; no photo keeps null fallback.
  - QA happy: use a synthetic fixture through gallery, remove it, use camera with a synthetic test capture, observe preview, complete, observe success image, open history and observe the same retained bytes/hash. Evidence `task-6/completion-preview.png|xml`, `success-photo.png|xml`, `history-photo.png|xml`, `manual.json`.
  - QA failure: cancel camera; inject Room failure with delete success and delete failure; simulate death after retain-before-commit; restart twice with first-delete failure; delete a referenced test file externally; inject stale inventory completion. PASS when compensation/reconciliation follows the bounded contract, missing-photo fallback renders without DB rewrite, referenced/unknown files are never swept, and no committed log is created with an internally deleted path. Evidence `task-6/failure-camera.json`, `failure-compensation.json`, `failure-reconcile.json`, `failure-missing.xml`.
  - Adversarial classes: navigation/coroutine cancellation during commit, process death, delete denial, canonical-path/symlink escape, unknown managed-directory file, referenced file, rapid replace/remove, camera recreation, duplicate meal-log ID, deleted cache source, misleading preview. Record file hashes/DAO reference sets in `task-6/adversarial.json`.
  - Cleanup: remove only synthetic task photos from app-private test directories and `/sdcard/Download/leftovers-expansion-task-6-*`, close scenarios, verify cache zero, keep no actual user photo, and record cleanup counts.
  - Commit: Y | `feat(completion): surface and preserve final photos` with footer `Plan: .omo/plans/leftovers-expansion.md`.

- [x] 7. Implement the single Vertex Gemini adapter with fixed models and one bounded logical-request budget
  - What to do / Must NOT do: Pin `google-genai==2.12.1` while retaining `openai==2.46.0` and `backend/app/openai_client.py`. Add only one narrow `GeminiAdapter` (and its injected transport/sleep seam) implementing the existing recipe/advice `AIAdapter`; do not add a provider registry/router/judge. Construct the official client in Vertex mode with `vertexai=True`, project from validated config, `location="global"`, ADC/no API key, and exactly `types.HttpOptions(api_version="v1", retry_options=types.HttpRetryOptions(attempts=1))`. Use response MIME `application/json` plus `model_json_schema(by_alias=True)`, and `types.Part.from_bytes(data=photo, mime_type=content_type)` for advice; never use Files/GCS. Fixed models: recipe primary `gemini-3.1-flash-lite`, fallback `gemini-3.5-flash`; advice primary `gemini-3.5-flash`, fallback `gemini-3.1-flash-lite`. Define one frozen request-owned budget value containing `schema_slots_used`, remaining primary-transient retry credits, `fallback_used`, and call counters. Create it once inside each logical HTTP request after request validation/quota admission; every transition returns a new value. Pass the current value explicitly into and the updated value explicitly out of every `parse_model_output` invocation, `AIAdapter` recipe/advice method, and provider-attempt context. The cached `GeminiAdapter`, cached SDK client, transport, sleeper, and app state hold zero mutable budget; do not use a mutable adapter field, module/class global, closure, `ContextVar`, thread-local, request-body key, or reset between schema slots. Each slot starts on primary. Exact transient classification remains `httpx.TimeoutException`, `httpx.ConnectError`, or `APIError` 429/500/503. While retry credits remain, an allowed primary transient sleeps 250 ms then 500 ms and retries primary in the same slot. Only the next allowed primary transient after both credits are exhausted may invoke fallback, once, in that same slot. Fallback is never retried: a valid fallback response succeeds; a schema/domain-invalid fallback response consumes that schema slot and permanently sets `fallback_used`, then any remaining repair slots invoke primary only; any fallback transport/provider/safety failure ends typed without another fallback. Primary schema/domain-invalid output consumes its slot and moves to primary in the next slot, never triggers fallback. Hard caps are ordinary primary/schema-slot invocations <=3, extra primary transient retries <=2, total primary <=5, fallback <=1, and total provider calls <=6. Cancellation is re-raised immediately. App auth/quota/input, non-transient provider errors, config/model-not-found, and safety never trigger fallback. Classify safety before JSON, consume quota once, expose generic typed bodies, log metadata only, and close async/sync clients in lifespan.
  - Parallelization: Wave 1 backend writer after T4 | Blocked by: T4 | Blocks: T8,T9 | Can parallelize with: T2,T3,T5,T6.
  - References: `backend/app/main.py:44-53,117-139,165-197,209-299`; `backend/app/openai_client.py:18-117`; `backend/app/config.py:9-18`; `backend/app/models.py:93-162,202-242`; `backend/app/quota.py`; `backend/requirements.txt:1-9`; tests `test_openai_client.py`, `test_api_boundaries.py`, `test_recipe_generation.py`, `test_cooking_advice.py`, `test_quota.py`; official SDK `https://googleapis.github.io/python-genai/genai.html`; ADC `https://cloud.google.com/docs/authentication/application-default-credentials`; model docs `https://ai.google.dev/gemini-api/docs/models/gemini-3.1-flash-lite?hl=en` and `https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash?hl=en`; locations `https://cloud.google.com/vertex-ai/generative-ai/docs/learn/locations`; retry source `https://raw.githubusercontent.com/googleapis/python-genai/main/google/genai/_api_client.py`.
  - Baseline then RED: run all current 66 backend tests before edits and capture `task-7/baseline.txt`. Add injected-fake tests for structured recipes/advice, alias schema, image bytes/MIME, locale/kind forwarding, safety metadata, each transient/error class, exact request-owned budget transitions and sleeps/calls across all schema attempts, quota once, log redaction, photo closure, and client lifespan closure. Add an async barrier test that launches at least two simultaneous byte-identical recipe requests through the same cached adapter/client, deliberately interleaves their transient/schema outcomes, and must currently expose missing ownership/isolation. Assert separate budget object identities and immutable before/after values; capture targeted failures in `task-7/red.txt` with no network/cloud call.
  - Acceptance criteria: `backend\.venv\Scripts\python.exe -m pytest -q backend\tests` and `backend\.venv\Scripts\python.exe -c "from app.main import create_app; assert create_app().title == 'LeftOVERS API'"` exit 0. Parameterized tests cover the exact error table and cancellation. Fake ledger proves: primary success uses one call; three primary schema-invalid outputs use exactly three primary calls, fallback 0, typed 422; two allowed primary transients then success use three primary calls and sleeps `[0.25,0.5]`; a third allowed primary transient invokes fallback once; fallback transport/error makes no further call; schema-invalid fallback consumes it and any remaining schema slots call primary only; no later event can call fallback again. Every trace satisfies ordinary-primary <=3, extra transient-primary <=2, primary-total <=5, fallback <=1, provider-total <=6, quota ==1. In the simultaneous identical-request test, each request independently observes its own `[0.25,0.5]` delay sequence and call caps under forced interleaving; exhausting or canceling one request steals no retry/fallback/schema credit and changes no counter or result in the other. Object/attribute inspection proves the adapter/client carry no budget state. Auth/config/input/non-transient/schema/domain/safety never trigger fallback. SDK inspection asserts Vertex/global/project, ADC/no API key, API version `v1`, `attempts=1`, schema aliases, byte part, and both closes.
  - QA happy: run local FastAPI with injected Gemini transport, authenticated synthetic English Meal/Korean Dessert request and synthetic PNG advice; validate HTTP 200 bodies through the real Pydantic validators and record sanitized call metadata only in `task-7/happy-fake.json`.
  - QA failure: parameterize timeout/connect/429/500/503, auth/permission/config/400/404/schema/domain/safety, and specifically `primary transient x3 -> fallback schema-invalid -> remaining primary repair`. Then interleave two identical requests where one exhausts transient credits/fallback while the other succeeds after its own retries, and cancel only one during its sleep. Assert independent immutable budget lineage, exact per-request slot/call/sleep/fallback-consumed state, no cross-request budget theft, closed upload/client, unchanged log canaries, and no exception text. Evidence `task-7/failure-matrix.json`, `concurrent-budget-isolation.json`, and `redacted-logs.txt`.
  - Adversarial classes: nested SDK retry, adapter/client mutable budget, shared global/closure/ContextVar/thread-local budget, budget reset between schema slots, concurrent request credit theft, cancellation leakage, fallback triggered by schema-invalid primary, fallback retried after schema-invalid fallback, repair continuing on fallback, call count >6, 429 app-quota bypass, fallback-on-safety, malformed JSON misclassified transient, cancellation during sleep, client leak, exception leakage, dynamic model override. Independent verifier replays the per-request fake ledger and writes `task-7/adversarial.json`.
  - Cleanup: close TestClient/lifespan, clear dependency overrides/captured logs, stop task uvicorn, delete synthetic temp image, free exact port, verify zero outbound DNS/HTTP and zero cloud command, and record counts.
  - Commit: Y | `feat(ai): use bounded Vertex Gemini adapter` with footer `Plan: .omo/plans/leftovers-expansion.md`; include backend/config/requirements/prompt/tests and local-only cloud-script contract edits, but no generated secret/config.

- [ ] 8. Correct privacy/publication docs and implement strict source, visual, and artifact validators
  - What to do / Must NOT do: Add `docs/privacy.md` and link it from README. Update active README/submission/provenance/demo wording from production GPT-5.6/OpenAI to Vertex ADC while retaining OpenAI code/secret/parity context. Disclose transient data, local final-photo retention, server non-persistence, quota-only Firestore, metadata logs, billable/best-effort inference, image cautions, `store=false` distinctions, and optional synthetic-share isolation. Also prepare the exact append-only original-plan text at `.omo/evidence/leftovers-expansion/task-8/superseding-vertex-publication-addendum.md`; it must cover remaining human+machine TLDR, scope/guardrails, verification/source/recapture rules, complete original T12 acceptance/QA/cleanup/authority flow, final verification, and success criteria, while explicitly preserving completed T1-T11 and GPT-5.6/OpenAI contribution as historical truth. Do not edit `.omo/plans/leftovers.md` yet; F1 reviews the exact addendum hash and completion later appends those approved bytes without alteration.
  - What to do / Must NOT do (proof and evidence-validator contract): In `scripts/submission_validation.py`, make `SubmissionRecord`, `ReleaseProof`, and `MediaEvidence` each require `schema_version: Literal[2]`, `ConfigDict(extra="forbid", strict=True)`, strict non-coercing string fields, and required lowercase 40-hex `publication_source_sha`; missing/v1 versions, unknown fields, integers/bytes/bools coerced to strings, and noncanonical SHA input fail closed. Require identical source SHAs when all three actual records exist, commit existence, ancestry to current HEAD, and source byte identity. The source SHA deliberately precedes evidence records to avoid self-reference. The complete post-source allowlist is exact: `.omo/evidence/leftovers-expansion/**`; `.omo/evidence/leftovers/task-12-publication/**`; `.omo/start-work/ledger.jsonl`; `.omo/boulder.json`; `.omo/plans/leftovers.md`; `dist/app-debug.apk.sha256`; `dist/release-debug-hooks.json`; `docs/submission.md`; `docs/demo-script.md`; `docs/asset-provenance.md`. No other `.omo` or `dist` path is allowed: in particular `.omo/plans/leftovers-expansion.md`, `.omo/drafts/leftovers-expansion.md`, test fixtures, verifier code, Android/backend/README/privacy, and any other tracked drift invalidate the source SHA. Keep the working tree clean. Do not delete old T12 evidence; mark it historical-only and remove it from active final claims.
  - What to do / Must NOT do (strict expansion visual validator): T8, not T12, implements and failing-first tests a strict `validate_expansion_visual_evidence(...)` entrypoint in the existing verifier surface. It accepts the expected `approved_source_sha`, APK SHA, device serial/model, and capture-window bounds, then validates `visual-manifest.json` as exactly 11 ordered states with integer `step_number` 1..11 and the exact canonical IDs from T12. Steps 1-3 and 5-11 contain exactly one capture; step 4 contains exactly the ordered `04a-onion-quarter` and `04b-onion-half` captures. Steps 1-5 require locale `en`, steps 6-10 `ko`, and step 11 `en`. Every state/capture repeats the same lowercase source SHA, APK SHA, device serial/model, and capture-run UUID; every RFC3339 UTC timestamp lies within the declared run window and is nondecreasing. Every PNG/XML path is unique, repo-relative, forward-slash only, nonempty, and rejects absolute/drive/UNC paths, backslashes, empty/`.`/`..` segments, case-fold duplicates, symlinks, and resolved escape outside the repo/evidence root. For every PNG, require file presence, exact declared lowercase SHA-256, the eight-byte PNG signature, a valid IHDR, and positive declared width/height equal to decoded dimensions. For every XML, require presence, exact declared lowercase SHA-256, successful parse with nonempty hierarchy, the same state/capture binding, and the canonical per-state required UI markers. Reject missing/extra/reordered/duplicate states or captures, reused paths/hashes, source/APK/device/locale/run mismatch, timestamp outside the run, stale pre-window evidence, PNG/XML hash/content/dimension mismatch, and XML bound to the wrong state. T12 may only produce evidence consumed by this already-implemented, already-RED/green-tested validator; T12 never edits its code or fixtures.
  - What to do / Must NOT do (canonical artifact record): T8 also implements/tests the exact consumer for `.omo/evidence/leftovers-expansion/task-12/artifact-manifest.json`; define `.omo/evidence/leftovers-expansion/task-12/` as the task root, and every named basename below resolves directly under it while its entry stores the full repo-relative path. The record has strict `schema_version: 2`, the expected `publication_source_sha`, and `files`, whose included set is exactly: `submission-draft.json`, `visual-manifest.json`, every PNG/XML path listed by the visual manifest, `manual.json`, `failure-stale.json`, `failure-restart.json`, `cleanup.json`, and `apk-reference.json` (the last strictly binds the repo-relative APK reference, lowercase APK SHA-256, and source SHA). `artifact-manifest.json` itself is never a `files` entry and contains no self-digest field. Each file entry is exactly `{ "path": <repo-relative-forward-slash-path>, "sha256": <lowercase-64-hex> }`; entries are case-sensitive Unicode-code-point ordinal sorted by canonical path, with no duplicates or extras, and each hash must match bytes on disk. The canonical record bytes are exactly UTF-8 without BOM of `json.dumps(record, ensure_ascii=False, allow_nan=False, sort_keys=True, separators=(",", ":"))` followed by exactly one LF and no other trailing whitespace. `artifact_record_digest` means lowercase SHA-256 of those exact `artifact-manifest.json` bytes. The digest covers the listed evidence through their path/hash entries but does not include the artifact-manifest file itself, avoiding a self-including digest paradox. T8 exposes one command that validates the visual manifest, included-file closure, canonical bytes, and returns that digest; every F1-F5 receipt must echo it exactly.
  - Parallelization: Wave 1 docs/verifier writer after T7 contract is fixed | Blocked by: T7 | Blocks: T9,T12 | Can parallelize with: T3,T5,T6 if files are disjoint.
  - References: `README.md:3-137`; `docs/submission.md:1-65`; `docs/demo-script.md:1-39`; `docs/asset-provenance.md:1-17`; `.omo/plans/leftovers.md:158-163`; `scripts/submission_validation.py:15-46,77-95,117-198,201-230`; `scripts/verify_submission.py:134-158`; `scripts/tests/test_verify_submission.py:143-323`; `scripts/tests/test_media_probe.py`; Python `json.dumps` canonicalization contract; PNG signature/IHDR and UIAutomator XML formats; historical release proof `.omo/evidence/leftovers/task-12-release-prep.json`; official OpenAI data controls `https://developers.openai.com/api/docs/guides/your-data` and Vertex data governance/ADC links cited above.
  - Baseline then RED: run `backend\.venv\Scripts\python.exe -m unittest scripts.tests.test_verify_submission scripts.tests.test_media_probe` and the current draft verifier to characterize old schema/claims. Before implementation, add temp-Git-repo/table fixtures that fail for: missing/invalid/nonexistent/non-ancestor/mismatched SHA; Submission/Release/Media missing version, v1, extra field, and coerced string; source drift/dirty tree; each missing/extra/reordered/duplicate canonical step; step-4 missing/reversed/extra subcapture; unsafe/duplicate/symlink path; absent PNG/XML; bad PNG signature/IHDR/dimensions/hash; malformed/empty/wrong-state XML/hash; source/APK/device/locale/run/timestamp mismatch; stale pre-window capture; artifact included-set omission/extra/order/hash error; noncanonical JSON/BOM/newline; and attempted self-entry/self-digest. Capture current false acceptance/missing validator in `task-8/red.txt`, then implement to green.
  - Acceptance criteria: verifier/media unittests and secret scan exit 0. Tests prove all three records are strict schema v2 with no extra/coercion, matching SHAs, exact allowlist, dirty/stale/ancestor/source rules, and immutable fixtures. A complete synthetic 11-step fixture passes with step 4's two subcaptures, real minimal PNG/IHDR bytes and parseable state-bound XML; every negative row above fails with a typed path/field reason. The artifact test asserts the exact included-file closure, ordinal forward-slash ordering, canonical UTF-8/sorted-key/compact/LF bytes, no self-entry/digest, and stable expected lowercase `artifact_record_digest`; changing any included byte changes the digest. Active docs reject current-runtime GPT-5.6/OpenAI claims and require privacy distinctions. An addendum contract test hashes the exact prepared file and requires all six future-active areas, explicit supersession of conflicting future language, preservation of completed T1-T11 bytes/history, truthful historical GPT-5.6/OpenAI wording, active Vertex runtime, three actual v2 records, recapture routing, and YouTube/Devpost authority gates. It fails if the addendum proposes editing a completed row.
  - QA happy: create a disposable temp Git fixture with a source commit, strict matching v2 records, the complete canonical visual fixture, and canonical artifact record; run the same production verifier command T12 will use and assert zero source/visual/artifact issues plus the expected artifact-record digest. Render/read README/privacy/submission end to end and capture `task-8/manual.json` with section and fixture hashes, not copied sensitive text.
  - QA failure: mutate one byte each in Android source, backend source, README, verifier, expansion plan, expansion draft, unnamed `.omo`/`dist`, proof SHA, PNG, XML, visual-manifest binding, artifact included file, and canonical artifact JSON; assert the exact field/path fails. Exercise v1/extra/coerced record inputs, all 11-step/step-4/path/image/XML/binding/stale rows, and prove an allowed expansion-evidence change does not mask simultaneous source drift. Evidence `task-8/failure-publication-source.json`, `failure-visual-manifest.json`, and `failure-artifact-manifest.json`.
  - Adversarial classes: SHA self-reference, artifact self-entry/self-digest, old schema downgrade, scalar coercion/extra fields, non-ancestor commit, path traversal/symlink/case-fold duplicate, allowed-prefix confusion, dirty worktree, missing/duplicate/reordered/collapsed state, swapped PNG/XML, forged dimension/hash, stale APK/media with fresh text, timestamp/run laundering, noncanonical JSON/newline, absolute username path, privacy overclaim, credit identifier leakage, misleading `store=false`. Record `task-8/adversarial.json`.
  - Cleanup: delete only disposable temp repos/files, leave historical tracked evidence untouched, prove no parent-worktree path was opened/scanned/staged, and record temp/process counts.
  - Commit: Y | `docs(privacy): bind Vertex publication evidence` with footer `Plan: .omo/plans/leftovers-expansion.md`; include verifier tests with documentation, not regenerated final T12 artifacts.

- [ ] 9. Pass the complete local fake-backed gate and prove no executor-issued live cloud invocation since A0
  - What to do / Must NOT do: At committed T1-T8 state, run all Android unit/lint/debug/release/androidTest compile, the explicitly filtered non-cloud connected suite (including `OnboardingSettingsTest` from T2), backend pytest, Docker build, publication/unit verifier, secret scan, fake Gemini matrices, and authority-guard fixture tests. Parse native XML/JSON/artifact bytes instead of trusting console summaries. Capture the onboarding/locale/kind/final-photo fake flow on the physical Flip3. If a defect appears, route it back to the owning todo, add the smallest characterization+RED test, fix/commit there, then rerun this gate on the new SHA. Do not run unfiltered connected tests, `RealCloudE2eTest`, gcloud/Firebase mutation/describe commands, actual Vertex/OpenAI requests, deploy, or scan/stage the parent worktree. The receipt may claim only that this executor issued no live Google Cloud invocation between A0 activation and T9 completion; it must not infer that no other actor changed the project.
  - Parallelization: Wave 2 exclusive integration owner | Blocked by: T1-T8 | Blocks: T10 | Can parallelize with: none (owns Gradle, device, Docker, evidence, and Git).
  - References: all T1-T8 references; `android/app/src/androidTest/java/com/junited31/leftovers/e2e/RealCloudE2eTest.kt:52-177` as the forbidden class; `scripts/scan_secrets.ps1`; Dockerfile; test inventories under `android/app/src/{test,androidTest}` and `backend/tests`.
  - Baseline then RED: baseline is each last green todo receipt. Before the full gate, run a negative harness that intentionally includes `RealCloudE2eTest` in a copied class list and assert the safety predicate rejects it before Gradle starts; inject one failing XML fixture and one secret-shaped non-Firebase fixture and prove the parsers/scanner fail. Store these expected failures in `task-9/red.txt`, then remove only the fixtures.
  - Acceptance criteria: run the exact Android source gate and safe connected command; parse all XML to zero failures/errors/skips and no `RealCloudE2eTest`; run backend pytest, exact-SHA Docker build/import, verifier unittests, and secret scan. After any owning-todo repair commit and final green rerun, require a clean product/source state and set `approved_source_sha=git rev-parse HEAD` before creating T9 evidence. Create `task-9/approved-source-manifest.json` containing that full SHA plus path and SHA-256 for every tracked product/config/script/doc file (all tracked paths outside `.omo/evidence/**`, `.omo/start-work/ledger.jsonl`, `.omo/boulder.json`, and named dist proof outputs); classification omission is a failure. T9's later evidence/ledger commit must change no manifested byte.
  - QA happy: clear app data on `R5CR91DXA8R`, run fake-backed Equipment -> Pantry suggestion Onion 1/4 then 1/2 -> Finish -> English Settings -> Korean -> Dessert -> exactly three candidates -> advice fixture -> completion final photo -> success/history -> English/restart. Evidence `task-9/local-flow.mp4`, state PNG/XML, `manual.json`, and connected result copy `local-connected-results/`.
  - QA failure: verify unsupported locale, no kind, mismatched candidate kind, transient fake budget, auth/config/schema/safety zero-fallback, canceled photo, missing retained photo, stale publication SHA, and secret fixture each fail closed without Room/photo/log/cloud side effects. Evidence `task-9/failure-matrix.json`.
  - Adversarial classes: unfiltered cloud test, misleading Gradle success, stale XML, skipped test, hidden SDK retry, dirty worktree, stale APK, sensitive log/body, concurrent device/Docker writer, repeated interruption. Independent read-only verifier checks exact SHA and writes `task-9/adversarial.json` with CONFIRMED/HIGH.
  - Cleanup: remove exact Docker tag/containers, stop task uvicorn/Gradle, restore device Wi-Fi/mobile/English, close app/test package, delete only `leftovers-expansion-task-9-*` device files and fake rows/photos, preserve ADB server if pre-existing, and record zero task residue plus the scoped executor-journal assertion of no executor-issued live cloud invocation since A0 in `task-9/cleanup.json`.
  - Commit: Y | `test(expansion): verify local Vertex-ready flow` with footer `Plan: .omo/plans/leftovers-expansion.md`; commit only sanitized task-9 evidence/ledger via explicit paths and retain `approved_source_sha` plus manifest hash in both.

- [ ] 10. Stop for explicit Google Cloud action-time confirmation
  - What to do / Must NOT do: Read-only revalidate T9 receipts, clean worktree, `approved_source_sha`, its manifest hash, current pre-question HEAD, and the scoped executor journal. Before asking, write nothing. Ask exactly: `로컬 구현, injected-fake Vertex 계약, Android local connected suite, backend pytest, Docker build, privacy/verifier 검증이 모두 통과했습니다. 다음 단계는 leftovers-019f706b에서 Vertex API 활성화, Cloud Run service identity IAM 변경, Cloud Run config/redeployment, 실제 Vertex 호출을 수행하는 외부 변경입니다. 지금 진행할까요?` Then stop. If declined/no answer, leave Git/Boulder/ledger untouched. Only after a direct affirmative reply may the agent create exactly `.omo/evidence/leftovers-expansion/task-10/authority.json`, append `.omo/start-work/ledger.jsonl`, and update `.omo/boulder.json`; the receipt records `approved_source_sha`, manifest hash, pre-authority HEAD, project/action scope, reply reference/time, and guard results without sensitive values. Commit exactly those three paths. Any fourth path or pre-authority drift invalidates authority. T11 independently byte-compares and revalidates before its first external call.
  - Parallelization: Wave 3 root authority gate | Blocked by: T9 | Blocks: T11 | Can parallelize with: none.
  - References: user request external-cloud section; `scripts/bootstrap_cloud.ps1:68-90,145-185`; `scripts/deploy_backend.ps1:95-163`; `RealCloudE2eTest.kt:52-177`; `task-9/` receipts; `.omo/start-work/ledger.jsonl` event format.
  - Baseline then RED: T9 owns the committed absent/stale/negative/wrong-project/changed-SHA/fourth-path guard fixtures. T10 performs no new test or write before asking. After affirmative, run only those local fixtures and embed their hashes/verdicts inside the single `authority.json`; do not create separate guard files or invoke a live entrypoint.
  - Acceptance criteria: pre-question Git bytes remain identical while waiting. After affirmative, `authority.json`, ledger, and Boulder all contain the same `approved_source_sha`, manifest hash, pre-authority HEAD, exact project, allowed action classes, and reply reference; `git diff --name-only <pre-authority-head>..HEAD` equals exactly the three named T10 paths. The fixture guard releases only `leftovers-019f706b` and five named action classes. No product/config/script/doc byte differs from the T9 manifest. The first T11 external command revalidates all predicates.
  - QA happy: after human authority, the agent runs the fixture-only guard, embeds its PASS in `authority.json`, and asserts only Vertex API enablement, Cloud Run service-identity IAM, Cloud Run config/redeploy, actual Vertex recipe/advice calls, and filtered `RealCloudE2eTest` are released for `leftovers-019f706b`.
  - QA failure: absent, stale-session, negative, ambiguous, wrong-project, changed-approved-SHA, changed manifest, fourth path, and changed product/config/script/doc fixtures all block before external invocation; their result hashes are embedded in `authority.json` after affirmative.
  - Adversarial classes: inherited approval, prompt ambiguity, wrong project, hidden deploy preflight real call, unfiltered connected suite, billing/credit leak, authority replay after SHA change, and any pre-question write. The agent records results only after affirmative authority.
  - Cleanup: before authority there is nothing to clean. After authority, remove only local guard temp files; no cloud/device/Docker operation occurs in T10.
  - Commit: Y after affirmative confirmation only | `chore(omo): record Vertex change approval` with footer `Plan: .omo/plans/leftovers-expansion.md`; stage exactly `.omo/evidence/leftovers-expansion/task-10/authority.json`, `.omo/start-work/ledger.jsonl`, and `.omo/boulder.json`. If confirmation is absent or any fourth path exists, do not commit and remain stopped.

- [ ] 11. Enable Vertex for the existing service, redeploy Gemini, and prove real text/image traffic
  - What to do / Must NOT do: Before the first external invocation, revalidate the exact three-path T10 diff and authority receipt, then recompute every T9 manifested product/config/script/doc hash from current HEAD and byte-compare it to `approved_source_sha`; any omission, mismatch, uncommitted change, or extra post-question path invalidates authority and returns to the owning todo -> T9 -> new T10. Execute, but do not edit, the already locally approved scripts to enable `aiplatform.googleapis.com`, resolve the exact Cloud Run identity, grant only it `roles/aiplatform.user`, set project/location, and deploy in `asia-northeast3`. Preserve auth/quota/service URL/secrets/history, omit the production OpenAI mount without deleting the secret, run structured English Meal/Korean Dessert and synthetic PNG advice, then only filtered `RealCloudE2eTest`. Do not patch product/config/scripts/docs under old authority, access another project, enable sharing, print credentials, or delete rollback resources.
  - Parallelization: Wave 3 exclusive cloud/device owner | Blocked by: confirmed T10 | Blocks: T12 | Can parallelize with: none.
  - References: `scripts/bootstrap_cloud.ps1:1-205`; `scripts/cloud_contracts.ps1:1-99`; `scripts/deploy_backend.ps1:1-163`; `backend/app/config.py`; `backend/app/main.py:149-170,209-299`; `backend/app/gemini_client.py` from T7; `RealCloudE2eTest.kt:52-177`; Firebase/quota tests; official Vertex quickstart/IAM `https://cloud.google.com/vertex-ai/generative-ai/docs/start/quickstart`.
  - Baseline then RED: no new TDD/product RED and no artificial source change. Re-run the exact previously committed T7/T9 fake-gcloud/Firebase RED-tested script guards for missing API/IAM/global config, wrong project, extra IAM, secret deletion, and missing receipt; bind their existing RED/green hashes in `task-11/pretested-guards.json`. After authority, use live invalid-auth, wrong-project guard dry-run, safety refusal, and quota exhaustion as failure probes; non-mutating describes are sanitized.
  - Acceptance criteria: preflight proves current manifested product/config/scripts/docs bytes equal `approved_source_sha`, the T10 diff is exactly its three named paths, and authority is current. Guarded commands then exit 0 only for `leftovers-019f706b`; postconditions prove exact IAM/region/location, no production OpenAI mount, retained OpenAI secret, health 200, invalid token 401, English Meal and Korean Dessert each return exactly three matching-kind candidates with no response locale, synthetic advice validates, quota data only, and metadata-only logs. Run only filtered `RealCloudE2eTest`; XML contains exactly that class with zero failures/errors/skips.
  - QA happy: install the cloud-configured debug APK and execute English onboarding, one kind-selected three-recipe response, synthetic cooking-photo advice, completion/final-photo/history/offline check; capture sanitized HTTP/model-role/call-count/device evidence in `task-11/cloud.json`, `cloud.png|xml`, and copied connected XML.
  - QA failure: expired token, request kind mismatch, safety-refusal fake/preflight, provider transient exhaustion, and app quota exhaustion must return their typed safe responses; app quota 429 must produce zero provider calls/fallback. Verify rollback revision remains available but do not switch/delete unless needed to recover failed deployment. Evidence `task-11/failure-cloud.json`.
  - Adversarial classes: wrong project/account, regional model assumption, broad IAM, secret deletion/leak, OpenAI mount retained accidentally, double quota, unfiltered connected suite, real user photo/data, log exception body, stale deployment revision, misleading 200. Independent verifier uses sanitized describes and writes `task-11/adversarial.json`.
  - Cleanup: delete only local token/config/temp synthetic-photo files; remove task device Downloads/anonymous test user where safe; restore network/app locale; leave requested API/IAM/revision/deployment and existing secrets intact; record exact retained vs removed resources without IDs/secrets in `task-11/cleanup.json`.
  - Commit: Y | `ops(cloud): cut production over to Vertex Gemini` with footer `Plan: .omo/plans/leftovers-expansion.md`; commit only sanitized task-11 evidence, ledger, and plan-state paths. Product/config/scripts/docs remain byte-identical to `approved_source_sha`; any needed edit invalidates authority and restarts the owning sequence.

- [ ] 12. Freeze publication_source_sha and capture the complete fresh bilingual physical-device flow
  - What to do / Must NOT do: T12 is evidence-only and never edits source, tests, fixtures, plans, docs, scripts, Gradle, backend, Android, deployment configuration, or the T8 validator. Revalidate T11 authority and manifest proof, set `publication_source_sha` exactly equal to T9 `approved_source_sha` (not evidence/current HEAD), and prove every current product/config/script/doc byte still matches it. Freeze `scripts/tests/fixtures/submission_draft.json` and every source/test file. Create the active draft only at `.omo/evidence/leftovers-expansion/task-12/submission-draft.json` and write the same SHA into it and `visual-manifest.json`. Expansion T12 does not create active release-proof or media-evidence records; original T12 later creates all three actual records with exact SHA equality. If a source/config/script/doc/fixture/validator defect is found, stop and return to its owner (validator defects to T8) -> T9 new `approved_source_sha` -> new T10 authority -> T11 -> T12. On `SM-F711N`, install an APK built from those source-identical bytes and execute only this canonical numbered flow, in order; `visual-manifest.json` must bind every numbered entry to its source/APK/device/locale/run plus original screenshot and matching UI XML hashes/dimensions/timestamps exactly as the already-tested T8 validator requires. Produce `apk-reference.json` with the repo-relative APK reference, lowercase installed-APK SHA-256, and source SHA. After every other included artifact is final, generate the canonical record only at `.omo/evidence/leftovers-expansion/task-12/artifact-manifest.json` from exactly the T8-defined included set and canonical serializer, run the unchanged T8 validator, and record its lowercase `artifact_record_digest`. Never list `artifact-manifest.json` inside itself or add a self-digest field. Do not collapse, reorder, skip, duplicate, or replace a state, and do not reuse old proof or actual user photos:
    1. `01-clear-app-data`: clear app data and capture the first clean launch.
    2. `02-english-equipment-onboarding`: complete the English Equipment onboarding state.
    3. `03-suggested-pantry`: choose and save a suggested pantry item through the existing validated form.
    4. `04-onion-quarter-half`: save Onion at `1/4`, then edit it to `1/2`; this one numbered entry contains two ordered screenshot/XML subcapture pairs, `04a-onion-quarter` and `04b-onion-half`, and Room values 250 then 500.
    5. `05-restart-persistence`: restart and prove onboarding, locale, and Onion `1/2` persistence.
    6. `06-switch-korean`: switch the application locale to Korean and capture the rendered state.
    7. `07-select-recipe-kind`: select one RecipeKind and prove its canonical wire value remains unchanged.
    8. `08-exactly-three-recipes`: generate and display exactly three matching-kind recipes.
    9. `09-cooking-photo-advice`: capture the cooking-photo advice result with safe synthetic imagery.
    10. `10-final-photo-completion-history`: add the final photo, complete the meal, and prove retained-photo display in History.
    11. `11-english-restart`: switch back to English, restart, and prove English plus persisted completion/history state.
  - Parallelization: Wave 4 exclusive device/evidence owner | Blocked by: T11 | Blocks: F1-F5 | Can parallelize with: none.
  - References: T1-T11 outputs; unchanged `scripts/submission_validation.py` strict v2/visual/artifact validator and green fixtures from T8; `docs/submission.md:5-30`; `PhotoLifecycle.kt`; all connected flow tests; historical-only `.omo/evidence/leftovers/task-12-*`, `dist/app-debug.apk.sha256`, `dist/release-debug-hooks.json`; exact device serial/model evidence from QA inventory.
  - Baseline then RED: no new failing product test or validator fixture. Re-run immutable T8 strict-record, visual-manifest, artifact-manifest, canonical-serialization, stale/freshness, and adversarial negative fixtures plus T9 guards; record their expected rejection output under `task-12/red.txt` without changing source/tests/fixtures. Before capture, prove the active task-12 evidence directory contains no prior active manifest/artifact files and old T12 paths cannot be selected. T12 only produces fresh evidence for the pretested validator.
  - Acceptance criteria: source gate, backend pytest, Docker build, safe local connected suite, secret scan, filtered `RealCloudE2eTest`, and immutable T8 verifier tests pass while current manifested bytes equal `approved_source_sha`. `publication_source_sha == approved_source_sha`, exists/is ancestor, and the unchanged production verifier reports zero drift against the exact active draft. No active expansion release/media record exists. `visual-manifest.json` contains exactly 11 top-level state entries with `step_number` integers 1 through 11 and exact `state_id` values `01-clear-app-data` through `11-english-restart` as enumerated above, with no gaps, duplicates, alternate ordering, or merged states. Every entry binds its original PNG and same-state XML by safe path, lowercase SHA-256, decoded dimensions/parsed state marker, timestamp/run, locale, source SHA, APK SHA, and device serial/model; step 4 alone binds the ordered `04a`/`04b` pairs. `artifact-manifest.json` is at the exact canonical path, includes exactly the active draft, visual manifest, all listed PNG/XML, manual/failure/cleanup JSON, and `apk-reference.json`, excludes itself, has ordinal path order and canonical bytes, and returns one lowercase `artifact_record_digest`. Missing/extra/reordered/stale/mismatched visual or artifact input fails.
  - QA happy: use ADB/Compose instrumentation and `adb exec-out screencap -p`/`uiautomator dump` to execute steps 1 through 11 exactly in order without hand-authored state claims, producing the screenshot/XML pair named by each manifest entry and both ordered pairs for step 4. Record app-data clearance, English Equipment onboarding, suggested pantry save, Room values 250 then 500, restart persistence, Korean switch, request locale+kind, response candidate kinds with no locale field, recipe count exactly 3, cooking-photo advice, retained-photo hash, completion/history path, restored-English locale tag, and final restart outcome in `task-12/manual.json`, each keyed to the same `step_number`/`state_id`. Finalize `failure-stale.json`, `failure-restart.json`, and `apk-reference.json`; artifact generation waits until `cleanup.json` is final, then uses the cleanup procedure below.
  - QA failure: compare one old T12 PNG/APK/media/proof and one deliberately stale copied capture against the source/visual/artifact contracts; the unchanged verifier rejects each without modifying source or fixtures. Kill/restart during locale and photo completion and prove persistence/ownership. Try a missing XML, swapped state XML, changed PNG byte, omitted/extra artifact entry, nonordinal path, noncanonical newline, and artifact self-entry in disposable copies; each fails. Evidence and adversarial results are sections of only `task-12/failure-stale.json` and `failure-restart.json`.
  - Adversarial classes: source/artifact self-reference, post-source forbidden drift, old evidence reuse, stale APK/capture, wrong device/locale/run, missing/duplicate/reordered screen, swapped XML state, bad PNG signature/dimension/hash, unsafe path/symlink, noncanonical JSON/order/newline, secret/notification/user photo exposure, console/XML mismatch, dirty worktree, interrupted capture. Replay T8 fixtures and record live preflight outcomes inside the two canonical failure JSON files; create no extra adversarial artifact outside the T8-defined included set.
  - Cleanup: remove only `/sdcard/Download/leftovers-expansion-*`, synthetic MediaStore rows, anonymous test user, app cache/retained test photos, task Docker image/processes; restore Wi-Fi/mobile and English; preserve deployed service/API/IAM/secrets; finalize zero temporary/device residue and all retained external resources in `task-12/cleanup.json`. With every included file now immutable, generate `artifact-manifest.json` last using the T8 canonical writer, run the unchanged T8 validator, require the returned lowercase digest twice identically, and store that digest only in the sanitized ledger/reviewer handoff rather than inside the record. Any subsequent included-file change invalidates cleanup/acceptance and requires artifact regeneration plus F1-F5 invalidation.
  - Commit: Y | `test(device): capture final bilingual expansion flow` with footer `Plan: .omo/plans/leftovers-expansion.md`; commit only `.omo/evidence/leftovers-expansion/task-12/**` and the sanitized ledger entry. Any other staged path aborts the commit and invokes the owning-todo restart sequence.

## Final verification wave
> Runs after all todos. Every row is review-only: reviewers may write their named reports under the expansion evidence root but may not edit source, plans, Boulder, ledger, deployment, or commit. All must approve. Surface results and wait for the user's explicit okay before the completion protocol.
> Failure routing is uniform. A reviewer never edits or recaptures underlying evidence. If any lane needs a T12 capture/manifest/artifact or F1 hands-on-QA evidence replacement, it fails and routes the request to the exclusive T12 evidence owner. That owner recaptures, commits only allowlisted evidence, regenerates canonical `artifact-manifest.json`, obtains a changed `artifact_record_digest`, and invalidates/reruns every F1-F5 lane. A product/config/script/doc defect returns to its owning todo -> T9 -> new T10 authority -> T11 -> T12, then reruns F1-F5. A verifier-contract defect returns to T8 and follows the same full chain.
- [ ] F1. Five-lane `omo:review-work` - invoke at the exact `publication_source_sha` plus the exact T12 `artifact_record_digest` and require PASS from goal/constraint, hands-on QA, code quality, security/privacy, and context/history. Compliance/context review the exact T8 addendum bytes/hash and confirm every future-active TLDR/scope/verification/T12/final-verification/success contract is superseded without modifying completed T1-T11 or erasing truthful historical GPT-5.6/OpenAI contribution. Reviewers first rerun the unchanged T8 validator, replay existing evidence without capture/patching, and each of the five receipts echoes the identical lowercase digest. Missing/replacement evidence routes to T12 and invalidates all lanes. Write only five exact-source-SHA/artifact-record-digest-bound receipts to `final/review-work.json`; any skipped/conditional lane fails.

- [ ] F2. Functional and design-integrity visual review - invoke `omo:visual-qa` with original-detail PNG/XML and the T8-validated canonical artifact record. Check onboarding clarity, hierarchy, reachability, five-tab gating, states, photo flow, accessibility, and rendering integrity. Result must be `PASS`, `HIGH`, blocking 0, bound to source/APK hashes and echo the exact `artifact_record_digest` in `final/f2-functional-visual.md`. Any missing/stale/corrected capture fails to the T12 recapture route and reruns all F1-F5; any UI source defect follows the full owning-todo chain.

- [ ] F3. Independent CJK and visual-precision review - a different `omo:visual-qa` reviewer inspects every Korean/bilingual state for glyphs, wrapping, truncation, labels/units, parity, mojibake/raw IDs, canonical wire values, targets/contrast, and English restoration. Result must be `PASS`, `HIGH`, blocking 0 and echo the exact `artifact_record_digest` with source/APK hashes in `final/f3-cjk-visual.md`. Evidence correction routes to T12 and reruns F1-F5; localization source defects follow the full owner chain. F2 cannot substitute.

- [ ] F4. Independent adversarial exact-SHA verifier - attack request-only locale/no-response-locale, RecipeKind, legacy leakage, request-owned concurrent retry budget/call caps, photo compensation/reconciliation, strict v2/no-coercion records, `approved_source_sha`/T10 three-path authority, visual/artifact canonicalization/self-entry, stale/allowlist/fixture/XML/APK/capture, and parent traversal. Write `final/f4-adversarial.json` with exact verdict `confirmed`, confidence >=0.95, `blockers: 0`, exact source/APK hashes, and the exact echoed `artifact_record_digest`. Any other/missing verdict or digest field fails. Evidence defects route to T12 and rerun F1-F5; verifier defects route to T8; other source defects route to their owner through the full chain.

- [ ] F5. Runtime-debug hypothesis audit - probe locale initialization/restart, request-vs-response locale, simultaneous-request budget isolation, fallback consumption after schema-invalid fallback, cancellation/process-death/delete-failure reconciliation, kind mismatch persistence, strict-record coercion, approved-source/T10 drift bypass, stale proof, canonical artifact digest, and console/XML disagreement. `final/f5-runtime-debug-audit.md` must declare exact verdict `PASS`, `blockers: 0`, echo the exact `artifact_record_digest`, and for every named hypothesis include observation, conclusion, `resolved: true`, exact source/APK hashes; any unresolved/missing hypothesis, blocker, or digest mismatch fails. Evidence defects route to T12 and rerun F1-F5; code/contract defects follow the full owning chain. Reviewer remains source/state read-only.

### Completion protocol (root-only, after F1-F5 and user acceptance)
1. Re-run the unchanged T8 validator, re-read all reports, and require identical source/APK hashes plus the exact same lowercase `artifact_record_digest` in every F1-F5 receipt. Then apply these lane-specific predicates: F1 has five `PASS` receipts; F2 is `PASS`, confidence `HIGH`, blockers 0; F3 is `PASS`, confidence `HIGH`, blockers 0; F4 is `confirmed`, confidence >=0.95, blockers 0; F5 is `PASS`, blockers 0, and every hypothesis is `resolved: true`. No generic all-lanes PASS coercion is allowed. Then commit only `.omo/evidence/leftovers-expansion/final/**` and its sanitized ledger receipt as `test(expansion): record final review approvals` with the expansion plan footer.
2. Do not edit any completed original T1-T11 row. Verify their bytes still match the A0 baseline, then append the exact F1-approved bytes from `task-8/superseding-vertex-publication-addendum.md` without alteration. Reject any hash mismatch. The addendum supersedes every conflicting future-active TLDR/scope/verification/T12/final-verification/success statement, makes Vertex active runtime/publication truth, requires the three same-SHA v2 records, preserves old proof as historical, retains YouTube/Devpost gates, and preserves GPT-5.6/OpenAI as historical contribution.
3. Atomically set `leftovers-expansion.status=completed`, `leftovers.status=active`, `active_work_id=leftovers`, keep original T12 unchecked, preserve worktree `D:/workspace/develop/LeftOVERS-impl` and every earlier session, and append the root session plus a sanitized transition ledger event. Commit only `.omo/plans/leftovers.md`, `.omo/boulder.json`, and `.omo/start-work/ledger.jsonl` as `chore(omo): resume final LeftOVERS publication` with both plan footers. Original T12 begins only after this expansion-completion commit exists.
4. Execute original T12 under its plan: require `ffmpeg`/`ffprobe`; clean-build debug/release from source-identical bytes; reinstall on the Flip3; re-record/watch/validate the full <180s narrated demo; regenerate all screenshots, `dist/app-debug.apk.sha256`, `dist/release-debug-hooks.json`, provenance, and the three actual v2 records under the named publication evidence root with identical `publication_source_sha`. Stop immediately before YouTube Public upload for human confirmation; after signed-out verification stop again immediately before Devpost Submit. Never use historical proof as fallback.

## Commit strategy
- A0's reviewed-plan commit and activation commit occur before implementation. Every later change is committed; implementation and its direct tests share one atomic Conventional Commit. Evidence/Boulder/ledger changes use separate `test(...)`, `docs(...)`, or `chore(omo)` commits only when they do not hide uncommitted source changes.
- Every T1-T12 commit message ends with exactly `Plan: .omo/plans/leftovers-expansion.md`. The completion transition uses both expansion/original-plan footers as specified; later original T12 publication commits use `Plan: .omo/plans/leftovers.md`.
- Before each commit: run its targeted green commands, `git diff --check`, secret scan when files could carry configuration/evidence, and `git status --short`; stage explicit files only. Never use `git add -A`/`.` from the parent worktree, never traverse `impl-link`, and never amend/rewrite the user's existing 15 commits.
- One Git writer at a time. Android UI commits follow T1,T2,T3,T5,T6 order; backend commits follow T4,T7; docs/verifier T8 follows T7; integration/cloud/device/evidence commits T9-T12 are serialized. Final reviewers edit only their named report paths and never commit; the completion protocol serializes report and transition commits.
- After T9 names `approved_source_sha`, no product/config/script/doc commit is allowed under that authority. T10's affirmative-only commit stages exactly authority.json, ledger, and Boulder; T11/T12 evidence commits may touch only their named evidence/ledger/plan-state paths. Any manifested-byte drift invalidates authority and restarts the owner -> T9 -> new T10 sequence.
- After `publication_source_sha` is frozen, the only permitted paths are `.omo/evidence/leftovers-expansion/**`, `.omo/evidence/leftovers/task-12-publication/**`, `.omo/start-work/ledger.jsonl`, `.omo/boulder.json`, `.omo/plans/leftovers.md`, `dist/app-debug.apk.sha256`, `dist/release-debug-hooks.json`, `docs/submission.md`, `docs/demo-script.md`, and `docs/asset-provenance.md`. Expansion plan/draft, test fixture, unnamed `.omo`/`dist`, or source changes invalidate the SHA. A required source fix returns to its owning todo and reruns T9 -> new T10 authority -> T11 -> T12 -> final reviews.
- Never commit secrets/tokens/API keys/billing or credit identifiers, local properties/credential files, actual user photos, temporary device/cache files, absolute local username paths, or sensitive parent-worktree evidence. Old T12 evidence remains historical and unchanged until original T12 intentionally replaces active publication references.
- No push/PR/merge/tag/release occurs in the expansion plan. Original T12 performs publication only after its regenerated local verifier and distinct action-time confirmations.

## Success criteria
- App data cleared on the Korean-locale Flip3 displays English Equipment onboarding first, hides bottom navigation until Equipment Continue and saved-Pantry Finish, then persists completion and exposes exactly five destinations with Settings containing equipment and English/Korean language controls.
- All 17 staples are local/bilingual and never save without quantity. Exact case-sensitive untrimmed/unnormalized canonical IDs localize regardless of typed/suggested origin; manually typed exact `onion` localizes, while `Onion ` and every non-exact custom name remain verbatim. Onion 0.25/0.5 survives restart; no provenance, conversion, catalog, or schema change exists.
- English/Korean changes render immediately and survive restart, while UUIDs/equipment IDs/enums/`g|ml|count` remain stable. Recipe Generate is disabled until exactly one of Meal/Drink/Snack/Dessert is selected.
- Android/FastAPI require request `locale` and `recipeKind`; exactly seven invalid contract rows fail with the specified pre-provider or primary-repair behavior. Locale drives generation but never appears in response schema/body. Every valid response has exactly three matching-kind candidates and preserves existing invariants. `RecipeSteps.recipeKind` has no default; only two legacy local decoders synthesize Meal; Room remains v1.
- Completion offers gallery/camera/preview/remove, success/history photo, and fallbacks. Before-entry cancellation has no effects; once in the narrow `NonCancellable` ownership section, a committed log retains its file. Transaction failure compensates; only delete failure/process death may leave a managed unreferenced orphan, which startup/retry reconciliation removes without touching referenced, unknown, symlinked, or out-of-directory files. No generated imagery exists.
- Production retains auth, one quota consumption, validation/safety, and metadata-only logs while using fixed Vertex models. A fresh frozen budget value is owned by each logical HTTP request and passed explicitly through parser/adapter/attempt calls; cached adapter/client state contains no mutable budget. Across three schema slots, ordinary primary <=3, extra transient primary <=2, total primary <=5, fallback <=1, provider total <=6. Fallback occurs only after allowed transient exhaustion; schema-invalid fallback consumes it and remaining repairs return to primary. Simultaneous identical requests independently receive their own delays/caps with no cross-request credit theft or cancellation leakage. Forbidden classes never trigger fallback.
- Local Android unit/lint/debug/release/androidTest compile, explicitly filtered non-cloud connected tests, backend pytest, Docker build, verifier tests, secret scan, fake provider matrix, and cleaned physical-device fake flow are green before the recorded human cloud confirmation. Evidence states only that this executor issued no live cloud invocation or RealCloud test since A0 activation; it makes no project-wide claim about other actors.
- T9 binds `approved_source_sha` and a complete tracked product/config/script/doc manifest. After affirmative authority, the T10 commit changes exactly authority.json, ledger, and Boulder. T11 byte-compares current manifested bytes to the approved SHA before external invocation; any other drift invalidates authority. Then only `leftovers-019f706b` external state changes and real structured/device proof passes.
- README/privacy/submission/provenance accurately disclose transient Vertex data categories, provider processing limitations, local final photo, server non-persistence, billable inference, best-effort fallback, synthetic-share isolation, and `store=false` distinctions. `SubmissionRecord`, `ReleaseProof`, and `MediaEvidence` all require strict `schema_version: Literal[2]`, forbid extras/coercion, and reject v1/missing/extra/coerced input. No billing/credit/secret/user-data value appears.
- Expansion T12 sets `publication_source_sha == approved_source_sha`; exact active draft/visual/artifact records bind it and all drift/stale proof fails closed through the unchanged T8 validator. Original T12 later creates all three actual strict-v2 records with the same SHA. Old artifacts are historical-only.
- T12 manifests exactly the numbered 1-11 flow: clear app data; English Equipment onboarding; suggested pantry; Onion `1/4` then `1/2`; restart persistence; switch Korean; select RecipeKind; exactly three recipes; cooking-photo advice; final-photo completion/history; switch English and restart. Every numbered entry has a state-bound PNG/XML pair, step 4 has its two ordered subcapture pairs, and no gap/duplicate/reorder/path/hash/dimension/XML/source/APK/device/locale/run/timestamp defect is accepted. The exact canonical record path is `.omo/evidence/leftovers-expansion/task-12/artifact-manifest.json`; its exact included-file closure, ordinal paths, lowercase hashes, sorted-key compact UTF-8 plus one-LF bytes, and non-self-inclusion yield one lowercase `artifact_record_digest`. Any T12 capture/manifest/artifact or F1 hands-on evidence replacement is performed and committed only by the T12 owner, changes that digest, and invalidates/reruns all F1-F5. Every final lane echoes the identical digest and passes its lane-specific predicate.
- The plan knowingly spends remaining time on expansion despite the July 21 5:00 PM PDT / July 22 9:00 AM KST deadline risk because the user explicitly required features before publication and granted no posting authority; no redundant prioritization question is asked.
- All source/evidence changes are committed with required footers. After final reviews and user acceptance, the completion protocol appends the comprehensive superseding addendum without changing completed T1-T11, marks expansion complete, reactivates original T12 unchecked, and only then regenerates APK/demo/screens/checksum and all three actual v2 records. Historical GPT-5.6/OpenAI contribution remains truthful history; Vertex governs all future-active publication claims. YouTube/Devpost remain blocked until fresh authority.
